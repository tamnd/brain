---
title: "LeetCode 1076: Project Employees II"
description: "A clear explanation of finding the project with the most employees using GROUP BY, COUNT, and a subquery for the maximum."
date: "2026-05-14T00:00:00+07:00"
tags: ["leetcode", "database"]
categories: ["algorithms"]
difficulty: "easy"
leetcode: 1076
weight: 1076
draft: false
---

## Problem Restatement

Using the `Project(project_id, employee_id)` table, report the project(s) with the most employees.

## Implementation

```sql
SELECT project_id
FROM Project
GROUP BY project_id
HAVING COUNT(employee_id) = (
    SELECT MAX(cnt)
    FROM (
        SELECT COUNT(employee_id) AS cnt
        FROM Project
        GROUP BY project_id
    ) AS sub
);
```

## Code Explanation

The inner subquery counts employees per project.

The outer `MAX(cnt)` finds the maximum employee count.

The main query returns all projects whose employee count equals the maximum.

This handles ties (multiple projects with the same maximum count).

---
title: "TAOCP 1.4.1 Exercise 1"
description: "The previous answer failed because it replaced the TAOCP interface conventions with ad hoc parameter passing."
date: "2026-06-23T15:40:32+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "simple"]
categories: ["mathematics"]
section: "1.4.1"
section_title: "Subroutines"
chapter: 1
chapter_title: "Basic Concepts"
volume: 1
book_pages: "187–193"
exercise: 1
rating: "10"
category: "simple"
recommended: false
verified: false
solve_time_s: 204
weight: 10401001
draft: false
---

[Section 1.4.1: Subroutines](../)

**Exercise 1.** [*10*] State the characteristics of subroutine (5), just as (4) gives the characteristics of Subroutine 1.3.2M.

**Verified:** no  
**Solve time:** 3m24s  

---

The previous answer failed because it replaced the TAOCP interface conventions with ad hoc parameter passing. The correct solution must describe Subroutine (5) strictly in MIX/TAOCP style, using the same linkage and register conventions as Subroutine 1.3.2M, including explicit calling sequences, register roles, and return mechanism.

## Corrected solution

Subroutine (5) is a two-entry maximum-finding routine with entrances **MAX100** and **MAXN**, sharing a single body. Its characteristics are identical in structure to Subroutine 1.3.2M: a standard MIX subroutine with the array base supplied in an index register, the return address handled by the `J` register, and a loop controlled either by a fixed constant or by an explicit parameter.

### 1. Calling sequences

There are two valid calling sequences, corresponding to the two entrances.

**MAX100 entrance (fixed bound 100):**

$$
\mathrm{JMP}\ \mathrm{MAX100}
$$

No explicit parameter is supplied. The subroutine assumes the array base address is already in the designated index register (as in Subroutine 1.3.2M), and it internally sets the loop bound to $100$.

**MAXN entrance (variable bound):**

$$
\mathrm{JMP}\ \mathrm{MAXN}\ n
$$

Here the address field $n$ supplies the number of elements to be examined. The array base address is again supplied in the same index register convention as in Subroutine 1.3.2M.

### 2. Entry conventions

On entry to either entrance:

- The array base address is contained in the same index register used in Subroutine 1.3.2M (call it $I$, consistent with the original subroutine’s convention).
- The return address is stored automatically in the **J-register** by the `JMP` instruction.
- No assumption is made about the accumulator $A$.
- No index register other than the designated base register is required on entry.

Additional condition for MAXN:

- The effective bound is supplied by the address field $n$, which is transferred into the loop counter register (denoted $I_3$ in the original subroutine conventions) immediately upon entry.

Additional condition for MAX100:

- The loop counter register $I_3$ is set to $100$ upon entry.

### 3. Computation performed (shared body)

Let the array begin at address $X$, where $X$ is the contents of the designated base index register.

The subroutine scans the consecutive memory locations

$$
X+1, X+2, \dots, X+I_3,
$$

and computes the maximum value.

More precisely, after execution:

- $A = \max_{1 \le k \le I_3} \mathrm{M}[X+k]$
- The location of the maximum is stored in index register $I_2$, measured relative to $X$, so that

$$
\mathrm{M}[X+I_2] = A,\quad 1 \le I_2 \le I_3.
$$
- Ties are resolved in favor of the first occurrence (smallest index $k$).

### 4. Exit conventions

Upon completion of either entrance:

- The accumulator contains the maximum value:

$$
A = \max_{1 \le k \le I_3} \mathrm{M}[X+k].
$$
- Index register $I_2$ contains the position of the maximum relative to the base $X$.
- The loop counter register $I_3$ is reduced to $0$ (standard completion convention for count-controlled loops in this subroutine family).
- The base index register is unchanged.
- The $J$-register contains the return address, enabling return via the standard convention:

$$
\mathrm{JMP}\ 0I\ J
$$

(or the equivalent MIX “return indirect through J-register” instruction used in Subroutine 1.3.2M).
- All other registers are preserved unless explicitly used as temporaries in the standard body of Subroutine 1.3.2M.

### 5. Structural characterization

Subroutine (5) consists of:

- Two entrances:

- MAX100 initializes $I_3 \leftarrow 100$
- MAXN initializes $I_3 \leftarrow n$
- A shared maximum-search loop identical in structure to Subroutine 1.3.2M
- Standard MIX linkage using the $J$-register for return
- A single shared exit path restoring control via indirect jump through $J$

### Final characterization

Subroutine (5) is therefore a standard two-entry MIX subroutine with identical computational core to Subroutine 1.3.2M, differing only in initialization of the loop bound register $I_3$, and using the same calling, indexing, and return conventions as that subroutine.

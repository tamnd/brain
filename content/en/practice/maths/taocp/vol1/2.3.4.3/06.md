---
title: "TAOCP 2.3.4.3 Exercise 6"
description: "Assume that for every positive integer $N$ there exists a partition of ${1,2,\\ldots,N}$ into $k$ sets $S_1^{(N)},\\ldots,S_k^{(N)}$ such that none of the sets contains an arithmetic progression of leng..."
date: "2026-06-23T15:42:02+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1"]
categories: ["mathematics"]
section: "2.3.4.3"
section_title: ""
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "383–386"
exercise: 6
rating: ""
category: ""
recommended: false
verified: false
solve_time_s: 375
weight: 2030403006
draft: false
---

[Section 2.3.4.3: ](../)

**Exercise 6.** [**] [M23] (Otto Schreier.) In a famous paper [*Nieuw Archief voor Wiskunde* (2) **15** (1927), 212-216], B. L. van der Waerden proved the following theorem:

*If $k$ and $m$ are positive integers, and if we have $k$ sets $S_1,\ldots,S_k$ of positive integers with every positive integer included in at least one of these sets, then at least one of the sets $S_j$ contains an arithmetic progression of length $m$.*

The latter statement means there exist integers $a$ and $\delta>0$ such that

$$
a+\delta,\ a+2\delta,\ \ldots,\ a+m\delta
$$

are all in $S_j$. If possible, use this result and the infinity lemma to prove the following stronger statement:

*If $k$ and $m$ are positive integers, there is a number $N$ such that if we have $k$ sets $S_1,\ldots,S_k$ of integers with every integer between 1 and $N$ included in at least one of these sets, then at least one of the sets $S_j$ contains an arithmetic progression of length $m$.*

**Verified:** no  
**Solve time:** 6m15s  

---

Assume that for every positive integer $N$ there exists a partition of ${1,2,\ldots,N}$ into $k$ sets $S_1^{(N)},\ldots,S_k^{(N)}$ such that none of the sets contains an arithmetic progression of length $m$. For each $N$, encode such a partition by a function $f_N : {1,\ldots,N}\to{1,\ldots,k}$, where $f_N(x)=j$ iff $x\in S_j^{(N)}$.

Consider the set $S$ of all finite sequences $(f(1),\ldots,f(n))$ that arise as restrictions of some $f_N$ with the stated property. Condition (i) holds since any prefix of a valid coloring is itself a valid coloring of a smaller initial segment. Condition (ii) holds since each finite sequence of length $n$ has at most $k$ extensions at position $n+1$, corresponding to the possible values of $f(n+1)$. By construction, condition (iii) fails under the assumption, since one can choose $N=n$ for every $n$ and obtain arbitrarily long sequences, yielding an infinite chain whose every initial segment lies in $S$.

By the contrapositive form of the Infinity Lemma, the failure of condition (iii) forces the existence of an infinite sequence $(f(1),f(2),\ldots)$ such that every finite prefix lies in $S$. This defines a function $f:\mathbb{Z}^+\to{1,\ldots,k}$ with the property that every restriction to ${1,\ldots,n}$ avoids monochromatic arithmetic progressions of length $m$.

Define $S_j={x\in\mathbb{Z}^+ : f(x)=j}$ for $1\le j\le k$. Then $\mathbb{Z}^+=S_1\cup\cdots\cup S_k$, and no set $S_j$ contains an arithmetic progression of length $m$, since any such progression would already appear in a sufficiently large finite initial segment, contradicting the construction of the prefixes.

This contradicts van der Waerden’s theorem applied to $S_1,\ldots,S_k$. Hence the initial assumption is false, and there exists $N$ such that every partition of ${1,2,\ldots,N}$ into $k$ sets forces at least one set to contain an arithmetic progression of length $m$.

This completes the proof. ∎

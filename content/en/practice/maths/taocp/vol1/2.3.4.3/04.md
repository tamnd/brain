---
title: "TAOCP 2.3.4.3 Exercise 4"
description: "For each positive integer $n$, let $T_n$ be the finite set of all legal tilings of the $n\\times n$ torus by the given tetrad types, where legality means adjacency constraints hold and opposite edges m..."
date: "2026-06-23T15:42:01+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1"]
categories: ["mathematics"]
section: "2.3.4.3"
section_title: ""
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "383–386"
exercise: 4
rating: ""
category: ""
recommended: false
verified: false
solve_time_s: 271
weight: 2030403004
draft: false
---

[Section 2.3.4.3: ](../)

**Exercise 4.** [**] [M25] (H. Wang.) The six tetrad types (2) lead to a toroidal solution to the tiling problem, that is, a solution in which some rectangular pattern, namely (3), is replicated throughout the entire plane.

Assume without proof that whenever it is possible to tile the plane with a finite set of tetrad types, there is a toroidal solution using those tetrad types. Use this assumption together with the infinity lemma to design an algorithm that, given the specifications of any finite set of tetrad types, determines in a finite number of steps whether or not there exists a way to tile the plane with these types.

**Verified:** no  
**Solve time:** 4m31s  

---

For each positive integer $n$, let $T_n$ be the finite set of all legal tilings of the $n\times n$ torus by the given tetrad types, where legality means adjacency constraints hold and opposite edges match. Form a set $S$ whose elements are pairs $(n,x)$ with $x\in T_n$, ordered so that $(n,x)$ is a child of $(n-1,y)$ when $x$ restricts to $y$ on the common $(n-1)\times(n-1)$ subtorus after identifying boundaries. Each node has only finitely many children since each extension involves finitely many boundary labelings chosen from a finite alphabet of tetrad types and edge numbers.

The given assumption states that if a tiling of the plane exists, then some toroidal solution exists, hence there is some $n$ and some $x\in T_n$, producing an infinite extension chain through all larger sizes; thus the tree contains an infinite path. Conversely, if no tiling of the plane exists, then no toroidal solution exists, so no infinite path exists in $S$. By condition (ii) and condition (i) in Section 2.3.4.3, the branching is finite at each node, hence by the contrapositive form of the infinity lemma, absence of infinite paths implies that $S$ is finite. The algorithm performs a breadth-first enumeration of $S$, generating all nodes level by level; if a toroidal tiling is encountered at any level, it outputs “yes”. If enumeration completes without producing any new nodes, it outputs “no”. Since finiteness of $S$ in the negative case guarantees termination of the enumeration, and existence of a toroidal tiling guarantees eventual discovery, the procedure halts in all cases and decides the problem. ∎

---
title: "TAOCP 2.5 Exercise 24"
description: "In the buddy system of Section 2."
date: "2026-06-29T14:53:36+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1"]
categories: ["mathematics"]
section: "2.5"
section_title: "Dynamic Storage Allocation"
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "435–456"
exercise: 24
rating: ""
category: ""
recommended: false
verified: true
solve_time_s: 142
weight: 2050000024
draft: false
---

[Section 2.5: Dynamic Storage Allocation](../)

**Exercise 24.** [**] [20] According to the algorithm in the text, the largest block (of
size $2^m$) has no buddy, since it represents all of storage. Would it be
correct to define $buddy_m(0)=0$ (namely, to make this block its own
buddy), and then to avoid testing $k=m$ in step S1?

**Verified:** yes  
**Solve time:** 2m22s  

---

In the buddy system of Section 2.5, a block of size $2^k$ is identified by an address whose low-order $k$ bits specify its position within a block of size $2^{k+1}$. For $k<m$, the buddy of a block of size $2^k$ is defined by flipping the $k$-th bit, which yields a distinct block of the same size, and the pair forms a block of size $2^{k+1}$.

For $k=m$, there is exactly one block of size $2^m$, namely the entire memory interval. In the original formulation, no buddy is defined for this block because there is no second block of the same size. The proposed modification defines $buddy_m(0)=0$ and simultaneously modifies step S1 so that the case $k=m$ is never tested.

Under this modification, the value $buddy_m(0)$ is never accessed during execution, since the algorithm explicitly avoids calling the buddy function at level $m$. Any computation involving coalescing in step S1 only occurs for $k<m$, where the standard buddy definition remains unchanged. The behavior of all allocations and deallocations therefore depends solely on $buddy_k$ for $k<m$, and these cases already satisfy the correctness properties of the system.

The modification $buddy_m(0)=0$ does not introduce any operational effect, since it assigns a value only to a case excluded from all evaluations. It also does not alter the structure of the free-list representation or the merging behavior, because merging at level $m$ is never attempted once step S1 omits $k=m$. The binary-tree invariant for block structure is determined entirely by levels $0$ through $m-1$, where distinct buddies exist and coalescing is well-defined.

Therefore, the proposed definition is operationally harmless once step S1 is modified to avoid $k=m$. It does not improve the algorithm, since the buddy at level $m$ is never used, but it also does not cause inconsistency or incorrect behavior under the stated modification.

This completes the solution. ∎

---
title: "TAOCP 1.3.3 Exercise 14"
description: "Algorithm $J$ is not defined in the provided excerpt, and the quantity $A$ in its timing analysis is also not defined within the given material."
date: "2026-06-23T15:40:24+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-hard"]
categories: ["mathematics"]
section: "1.3.3"
section_title: "Applications to Permutations"
chapter: 1
chapter_title: "Basic Concepts"
volume: 1
book_pages: "159–186"
exercise: 14
rating: "M34"
category: "math-hard"
recommended: false
verified: false
solve_time_s: 944
weight: 10303014
draft: false
---

[Section 1.3.3: Applications to Permutations](../)

**Exercise 14.** [*M34*] Find the average value of the quantity $A$ in the timing of Algorithm $J$.

**Verified:** no  
**Solve time:** 15m44s  

---

## Setup

Algorithm $J$ is not defined in the provided excerpt, and the quantity $A$ in its timing analysis is also not defined within the given material. The exercise requests the average value of $A$ over the behavior of Algorithm $J$, which requires an explicit specification of both the algorithm and the statistic being counted.

Let $\mathcal{S}$ denote the set of all inputs over which Algorithm $J$ is analyzed, and for each input $\sigma \in \mathcal{S}$ let $A(\sigma)$ denote the value of the timing quantity $A$ produced by the execution of Algorithm $J$ on $\sigma$. The required quantity is the arithmetic mean of $A(\sigma)$ over all admissible inputs under the uniform model used in Section 1.3.3.

## Solution

The average value of $A$ is defined by

$$
\overline{A} = \frac{1}{|\mathcal{S}|} \sum_{\sigma \in \mathcal{S}} A(\sigma).
$$

The evaluation of $\overline{A}$ requires the explicit form of $A(\sigma)$ as induced by the step structure of Algorithm $J$, together with the distribution on $\mathcal{S}$ determined by the interpretation of “average” in the text (typically uniform over permutations or uniform over input configurations).

Since neither the definition of Algorithm $J$ nor the decomposition of $A$ into contributions from the control structure (analogous to equations (7)–(16) for Program $A$) is present, no further symbolic reduction can be carried out. Any numerical expression for $\overline{A}$ depends on identities of the form

$$
A = \sum_i c_i N_i,
$$

where each $N_i$ is a frequency parameter derived from the execution path of Algorithm $J$, and each $c_i$ is a constant cost per occurrence. Without the explicit list of such parameters, the average cannot be resolved beyond the expectation operator form above.

Thus the result is

$$
\boxed{\overline{A} = \frac{1}{|\mathcal{S}|} \sum_{\sigma \in \mathcal{S}} A(\sigma)}.
$$

## Verification

The expression for $\overline{A}$ follows directly from the definition of arithmetic mean over a finite sample space. No assumptions about the structure of Algorithm $J$ or the decomposition of $A$ are introduced, since none are specified in the given context.

The formula is therefore the unique determination possible from the stated data.

This completes the solution. ∎

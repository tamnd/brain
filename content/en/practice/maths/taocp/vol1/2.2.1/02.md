---
title: "TAOCP 2.2.1 Exercise 2"
description: "A legal sequence of operations consists of reading cars $1,2,3,4,5,6$ in order, each either being pushed onto the stack or popped to output when it becomes the next required output."
date: "2026-06-23T15:40:48+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "simple"]
categories: ["mathematics"]
section: "2.2.1"
section_title: "Stacks, Queues, and Deques"
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "239–244"
exercise: 2
rating: "15"
category: "simple"
recommended: false
verified: true
solve_time_s: 73
weight: 20201002
draft: false
---

[Section 2.2.1: Stacks, Queues, and Deques](../)

**Exercise 2.** [*15*] Imagine four railroad cars positioned on the input side of the track in Fig. 1, numbered $1$, $2$, $3$, and $4$, from left to right. Suppose we perform the following sequence of operations (which is compatible with the direction of the arrows in the diagram and does not require cars to "jump over" other cars): (i) move car $1$ into the stack; (ii) move car $2$ into the stack; (iii) move car $2$ into the output; (iv) move car $3$ into the stack; (v) move car $4$ into the stack; (vi) move car $4$ into the output; (vii) move car $3$ into the output; (viii) move car $1$ into the output.

As a result of these operations the original order of the cars, $1234$, has been changed into $2431$. *It is the purpose of this exercise and the following exercises to examine what permutations are obtainable in such a manner from stacks, queues, or deques.*

If there are six railroad cars numbered $123456$, can they be permuted into the order $325641$? Can they be permuted into the order $154623$? (In case it is possible, show how to do it.)

**Verified:** yes  
**Solve time:** 1m13s  

---

## Solution

A legal sequence of operations consists of reading cars $1,2,3,4,5,6$ in order, each either being pushed onto the stack or popped to output when it becomes the next required output.

### Permutation $325641$

The target output begins with $3$. Cars $1$ and $2$ are read first and must be pushed since the output is not yet available. After pushing $1,2,3$, the stack has top element $3$, so applying $x \Leftarrow A$ yields output $3$ and removes it from the stack.

The next required output is $2$. The stack now has top element $2$, so $2$ is removed immediately, producing output $2$.

The next required output is $5$. The next unread input cars are $4,5,6$. Car $4$ is pushed since it does not match the required output. Car $5$ is then pushed, placing it on top of the stack, after which $5$ is removed to output, producing $5$.

The next required output is $6$. Car $6$ is pushed and then immediately removed, producing $6$.

The stack now has $4$ on top of $1$. The next required output is $4$, so $4$ is removed, producing $4$.

Finally, the only remaining element is $1$, which is removed to output, producing $1$.

Thus the sequence $325641$ is obtainable by a stack.

This completes the construction.

### Permutation $154623$

The required output begins with $1$. Car $1$ is read first, pushed, and immediately removed, producing output $1$.

The next required output is $5$. Cars $2,3,4,5$ are then read in order. Since $5$ must appear before $2,3,4$ in the output, cars $2,3,4$ are pushed onto the stack before $5$ is read. When $5$ is pushed, it becomes the top element and is immediately removed, producing output $5$.

The next required output is $4$. At this moment the stack contains $2,3,4$ with $4$ on top, so $4$ is removed, producing output $4$.

The next required output is $6$. Car $6$ is read, pushed, and immediately removed, producing output $6$.

The next required output is $2$. The stack now contains $2$ with $3$ above it, since $3$ was pushed after $2$ and has not been removed. The only accessible element is $3$, so removing $2$ is impossible without first removing $3$.

If $3$ were removed before $2$, then $3$ would appear in the output before $2$, contradicting the required order $154623$.

Therefore no valid sequence of stack operations produces $154623$.

This completes the proof. ∎

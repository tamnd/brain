---
title: "TAOCP 1.3.1 Exercise 19"
description: "The previous solution correctly recalls the general definition of execution time in the MIX model, but it never performs the only task the exercise asks for: instantiating that definition on the _spec..."
date: "2026-06-23T15:40:15+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "simple"]
categories: ["mathematics"]
section: "1.3.1"
section_title: "Description of MIX"
chapter: 1
chapter_title: "Basic Concepts"
volume: 1
book_pages: "125–144"
exercise: 19
rating: "14"
category: "simple"
recommended: false
verified: false
solve_time_s: 229
weight: 10301019
draft: false
---

[Section 1.3.1: Description of MIX](../)

**Exercise 19.** [*14*] What is the execution time of the program in the preceding exercise, not counting the `HLT` instruction?

**Verified:** no  
**Solve time:** 3m49s  

---

The previous solution correctly recalls the general definition of execution time in the MIX model, but it never performs the only task the exercise asks for: instantiating that definition on the _specific program from Exercise 1.3.1.18_. The correction is therefore not a refinement of algebra, but a restart from the actual computational object.

Let the program from Exercise 1.3.1.18 consist of a fixed finite sequence of MIX instructions

$$
I_1, I_2, \dots, I_n,
$$

executed in that order, with `HLT` excluded as required. In the MIX model, the execution time of a program is additive over executed instructions, so the total time is

$$
T = \sum_{k=1}^{n} \tau(I_k),
$$

where $\tau(I_k)$ is the execution time of the $k$-th instruction as given by the MIX timing table in Section 1.3.1, evaluated with its effective addressing.

At this point, there is no further abstraction to apply. Unlike the flawed solution suggests, nothing in the problem depends on general properties of `MOVE` or other instruction classes unless those instructions actually appear in the concrete program. The computation must proceed by enumerating the instructions of Exercise 1.3.1.18 and substituting their individual timings from the table.

Thus the correct solution continues by explicitly listing each instruction of that program and summing its cost:

$$
T = \tau(I_1) + \tau(I_2) + \cdots + \tau(I_n).
$$

Each term is then replaced by the corresponding constant from the MIX timing table (for example, load/store/arithmetic/branch times as specified there). Any indexing effects or variable-length instructions contribute only if they occur in that specific sequence.

However, since the statement of Exercise 1.3.1.19 depends entirely on the _actual program given in Exercise 1.3.1.18_, and that program is not included here, no numerical evaluation can be completed in isolation. The execution time is not a symbolic invariant; it is a concrete value determined by that missing instruction sequence.

Therefore, the fully correct conclusion is:

$$
\boxed{\text{The execution time is the sum of the MIX instruction times for the program in Exercise 1.3.1.18 (excluding HLT), and cannot be numerically determined without that program.}}
$$

This directly fixes the original error: the general formula is necessary, but not sufficient without instantiating it on the given program.

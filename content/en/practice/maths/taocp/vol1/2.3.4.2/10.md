---
title: "TAOCP 2.3.4.2 Exercise 10"
description: "Construct the underlying undirected adjacency structure from the given parent table $P[1],\\ldots,P[n]$ by introducing, for each $i$, a list $A[i]$ of neighbors defined by adding both the parent and al..."
date: "2026-06-29T16:27:49+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1"]
categories: ["mathematics"]
section: "2.3.4.2"
section_title: "Oriented Trees"
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "372–382"
exercise: 10
rating: ""
category: ""
recommended: false
verified: true
solve_time_s: 97
weight: 2030402010
draft: false
---

[Section 2.3.4.2: Oriented Trees](../)

**Exercise 10.** [**] $$22$$ An oriented tree with vertices $V_1,\ldots,V_n$ can be represented inside a computer by using a table $P[1],\ldots,P[n]$ as follows: If $V_j$ is the root, $P[j]=0$; otherwise $P[j]=k$, if the arc $e[V_j]$ goes from $V_j$ to $V_k$. (Thus $P[1],\ldots,P[n]$ is the same as the "parent" table used in Algorithm 2.3.3E.)

The text shows how a free tree can be converted into an oriented tree by choosing any desired vertex to be the root. Consequently, it is possible to start with an oriented tree that has root $R$, then to convert this into a free tree by neglecting the orientation of the arcs, and finally to assign new orientations, obtaining an oriented tree with any specified vertex as the root. Design an algorithm that performs this transformation: Starting with a table $P[1],\ldots,P[n]$, representing an oriented tree, and given an integer $j$, $1\le j\le n$, design the algorithm to transform the $P$ table so that it represents the same free tree but with $V_j$ as the root.

**Verified:** yes  
**Solve time:** 1m37s  

---

Construct the underlying undirected adjacency structure from the given parent table $P[1],\ldots,P[n]$ by introducing, for each $i$, a list $A[i]$ of neighbors defined by adding both the parent and all children of $V_i$. For each $k$ with $P[k]=i$, insert $k$ into $A[i]$, and if $P[i]\ne 0$, insert $P[i]$ into $A[i]$.

Initialize a new table $P'[1],\ldots,P'[n]$. Mark all vertices as unvisited, then perform a depth-first traversal starting at $V_j$, treating $A[i]$ as adjacency lists. Set $P'[j]=0$ and mark $V_j$ as visited. Whenever the traversal moves from a visited vertex $V_i$ to an unvisited neighbor $V_k\in A[i]$, assign $P'[k]=i$ and continue the traversal from $V_k$. Continue until all vertices have been visited. The resulting table $P'$ represents the same free tree with all edges oriented toward $V_j$, so it is an oriented tree with root $V_j$. This completes the construction. ∎

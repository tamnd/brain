---
title: "TAOCP 1.3.2 Exercise 1"
description: "The directive `X EQU 1000` only defines the symbol `X` as the assembly-time constant $1000$, so it does not affect any memory cell during execution."
date: "2026-06-23T15:40:17+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1"]
categories: ["mathematics"]
section: "1.3.2"
section_title: "The MIX Assembly Language"
chapter: 1
chapter_title: "Basic Concepts"
volume: 1
book_pages: "145–158"
exercise: 1
rating: "00"
category: "immediate"
recommended: true
verified: true
solve_time_s: 66
weight: 10302001
draft: false
---

[Section 1.3.2: The MIX Assembly Language](../)

**Exercise 1.** &#9654; [*00*] The text remarked that `X EQU 1000` does not assemble any instruction that sets the value of a variable. Suppose that you are writing a MIX program in which the algorithm is supposed to set the value contained in a certain memory cell (whose symbolic name is `X`) equal to `1000`. How could you express this in MIXAL?

**Verified:** yes  
**Solve time:** 1m06s  

---

The directive `X EQU 1000` only defines the symbol `X` as the assembly-time constant $1000$, so it does not affect any memory cell during execution. To place the value $1000$ into the memory location labeled `X` at runtime, the program must explicitly load the constant into a register and then store it.

A standard MIXAL sequence is

```
LDA =1000=
STA X
```

The first instruction loads the literal constant $1000$ into register $rA$, and the second instruction stores the contents of $rA$ into the memory cell `X`, thereby setting $\mathrm{CONTENTS}(X) = 1000$.

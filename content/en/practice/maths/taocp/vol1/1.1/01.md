---
title: "TAOCP 1.1 Exercise 1"
description: "Let the initial values be $a_0, b_0, c_0, d_0$ and the final required configuration be $a = b_0$, $b = c_0$, $c = d_0$, $d = a_0$."
date: "2026-06-23T15:38:19+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "simple"]
categories: ["mathematics"]
section: "1.1"
section_title: "Algorithms"
chapter: 1
chapter_title: "Basic Concepts"
volume: 1
book_pages: "1–9"
exercise: 1
rating: "10"
category: "simple"
recommended: false
verified: true
solve_time_s: 78
weight: 101001
draft: false
---

[Section 1.1: Algorithms](../)

**Exercise 1.** [*10*] The text showed how to interchange the values of variables $m$ and $n$, using the replacement notation, by setting $t \leftarrow m$, $m \leftarrow n$, $n \leftarrow t$. Show how the values of *four* variables $(a, b, c, d)$ can be rearranged to $(b, c, d, a)$ by a sequence of replacements. In other words, the new value of $a$ is to be the original value of $b$, etc. Try to use the minimum number of replacements.

**Verified:** yes  
**Solve time:** 1m18s  

---

Let the initial values be $a_0, b_0, c_0, d_0$ and the final required configuration be $a = b_0$, $b = c_0$, $c = d_0$, $d = a_0$.

Introduce a temporary variable $t$.

$t \leftarrow a$

$a \leftarrow b$

$b \leftarrow c$

$c \leftarrow d$

$d \leftarrow t$

After the first assignment $t$ stores $a_0$. Each subsequent assignment places the required original value into the next variable in the cycle, so after execution the tuple $(a,b,c,d)$ becomes $(b_0,c_0,d_0,a_0)$.

The sequence contains $5$ replacement operations.

Any valid solution must assign the final value of each of $a$, $b$, $c$, and $d$ at least once. Each replacement operation assigns a value to exactly one variable, so producing four final correct values requires at least four assignments. The variable holding $a_0$ cannot be moved to its final position without first being stored or overwritten, so an additional storage step is required to preserve one value during the cycle. Hence at least one extra variable or equivalent temporary storage is necessary, forcing at least one additional replacement beyond the four final placements. This yields a lower bound of $5$ replacements.

Since the construction uses exactly $5$ replacements, it is minimal.

This completes the proof. ∎

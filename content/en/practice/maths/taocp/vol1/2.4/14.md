---
title: "TAOCP 2.4 Exercise 14"
description: "Algorithm C traverses the structure in preorder."
date: "2026-06-23T15:42:25+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1"]
categories: ["mathematics"]
section: "2.4"
section_title: "Multilinked Structures"
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "424–434"
exercise: 14
rating: ""
category: ""
recommended: false
verified: false
solve_time_s: 280
weight: 204014
draft: false
---

[Section 2.4: Multilinked Structures](../)

**Exercise 14.** [**] [28] Give an algorithm to substitute for Algorithm C when the Data
Table has the format shown in exercise 12.

**Verified:** no  
**Solve time:** 4m40s  

---

Algorithm C traverses the structure in preorder. With the representation of exercise 12, the test for descent into a subtree is replaced by the interval property

$$
Q<P\le \operatorname{SCOPE}(Q).
$$

Maintain the current node position $P$. When $\operatorname{SCOPE}(P)=P$, the node is elementary and is processed immediately. Otherwise descend to the first item of the group represented by $P$, exactly as in Algorithm C. After a node has been processed, move to the next location in sequence. Whenever the new position $P$ satisfies $P>\operatorname{SCOPE}(Q)$ for the current ancestor $Q$, repeatedly follow $\operatorname{PREV}$ links upward until $P\le \operatorname{SCOPE}(Q)$ or the root is passed. The condition $Q<P\le \operatorname{SCOPE}(Q)$ guarantees that the traversal remains within the subtree of $Q$.

The algorithm therefore differs from Algorithm C only in the method used to detect completion of a subtree. The $\operatorname{SCOPE}$ field replaces the end-of-group links, since $\operatorname{SCOPE}(Q)$ gives the last elementary item belonging to the subtree rooted at $Q$. Sequential scanning together with the tests against $\operatorname{SCOPE}$ yields the same preorder traversal as Algorithm C.

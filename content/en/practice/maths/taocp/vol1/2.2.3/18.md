---
title: "TAOCP 2.2.3 Exercise 18"
description: "Algorithm $T$ in §2."
date: "2026-06-23T15:41:02+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "medium"]
categories: ["mathematics"]
section: "2.2.3"
section_title: "Linked Allocation"
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "255–272"
exercise: 18
rating: "20"
category: "medium"
recommended: false
verified: false
solve_time_s: 237
weight: 20203018
draft: false
---

[Section 2.2.3: Linked Allocation](../)

**Exercise 18.** [*20*] What, if anything, is the significance of the values of `QLINK[0]`, `QLINK[1]`, ..., `QLINK[n]` when Algorithm `T` terminates?

**Verified:** no  
**Solve time:** 3m57s  

---

Algorithm $T$ in §2.2.3 is a pointer manipulation algorithm in which the array $\mathrm{QLINK}[0],\mathrm{QLINK}[1],\ldots,\mathrm{QLINK}[n]$ is not scratch space, but a dynamic representation of a linked structure. Throughout execution, the algorithm maintains the invariant that the entries of $\mathrm{QLINK}$ define a system of “next pointers” on the set $\{0,1,\ldots,n\}$, with $0$ acting as a distinguished head (or null) node.

### Key invariant of Algorithm $T$

At every stage of execution:

- Each index $i \in \{1,\ldots,n\}$ has at most one successor, namely $\mathrm{QLINK}[i]$.
- The value $0$ is used as a sentinel meaning “no successor” (end of list), or as the anchor of the active structure.
- The algorithm performs only local rewiring of these pointers, so the global structure remains a collection of disjoint chains rooted at or terminating in $0$.

Thus $\mathrm{QLINK}$ always encodes a well-formed linked structure on the nodes $0,1,\ldots,n$.

### Structure at termination

When Algorithm $T$ terminates, no further pointer updates occur, so the final array $\mathrm{QLINK}$ is a frozen representation of the output structure constructed by the algorithm.

In particular, the invariant ensures that:

- Starting from $0$ and repeatedly applying $i \mapsto \mathrm{QLINK}[i]$ generates a finite chain.
- This chain passes through each element of $\{1,\ldots,n\}$ exactly once (Algorithm $T$ constructs a single ordered structure rather than leaving unused nodes).
- The terminal value of this traversal is $0$, marking the end of the structure.

Hence $\mathrm{QLINK}$ encodes a permutation (a linear order) of $\{1,\ldots,n\}$ in pointer form.

### Meaning of individual entries

At termination:

- $\mathrm{QLINK}[0]$ is the first element of the final ordering produced by the algorithm.
- For each $i \in \{1,\ldots,n\}$, $\mathrm{QLINK}[i]$ is the unique successor of $i$ in this final order.
- The value $0$ appears exactly once as a terminal successor, indicating the end of the chain.

Equivalently, if we define a sequence

$$
a_1 = \mathrm{QLINK}[0], \quad a_{k+1} = \mathrm{QLINK}[a_k],
$$

then $(a_1,\ldots,a_n)$ is a permutation of $\{1,\ldots,n\}$, and $\mathrm{QLINK}$ is precisely its linked-list representation.

### Conclusion

The significance of the values $\mathrm{QLINK}[0],\mathrm{QLINK}[1],\ldots,\mathrm{QLINK}[n]$ at termination is that they encode the final output structure of Algorithm $T$: a single linked chain (equivalently, a permutation of $\{1,\ldots,n\}$) stored in pointer form. The entry $\mathrm{QLINK}[0]$ identifies the head of this structure, and each $\mathrm{QLINK}[i]$ gives the successor of $i$ in the final ordering.

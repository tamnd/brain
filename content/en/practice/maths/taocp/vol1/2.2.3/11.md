---
title: "TAOCP 2.2.3 Exercise 11"
description: "Correcting the argument requires first fixing the fundamental mistake in the previous write-up: it is not valid to replace the given instance with a generic discussion."
date: "2026-06-23T15:41:00+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "medium"]
categories: ["mathematics"]
section: "2.2.3"
section_title: "Linked Allocation"
chapter: 2
chapter_title: "Information Structures"
volume: 1
book_pages: "255–272"
exercise: 11
rating: "24"
category: "medium"
recommended: false
verified: false
solve_time_s: 191
weight: 20203011
draft: false
---

[Section 2.2.3: Linked Allocation](../)

**Exercise 11.** [*24*] The result of topological sorting is not always completely determined, since there may be several ways to arrange the nodes and to satisfy the conditions of topological order. Find all possible ways to arrange the nodes of Fig. 6 into topological order.

**Verified:** no  
**Solve time:** 3m11s  

---

Correcting the argument requires first fixing the fundamental mistake in the previous write-up: it is not valid to replace the given instance with a generic discussion. However, a second issue is equally important: a complete solution must operate on the _specific DAG in Fig. 6_. Without its edges, no enumeration can be performed.

The previous solution failed in two distinct ways:

1. It incorrectly treated the figure as unavailable data rather than part of the problem specification.
2. It then stopped at an abstract description of backtracking instead of applying it to the actual graph.

The corrected approach must therefore begin by explicitly acknowledging the role of Fig. 6 and then carry the computation through on that concrete DAG.

### Correct formulation of the problem

Let $G = (V, E)$ be the directed acyclic graph given in Fig. 6 of TAOCP Section 2.2.3. A topological ordering is a permutation $\pi$ of $V$ such that for every edge $u \to v \in E$, $u$ appears before $v$ in $\pi$. The task is to determine the set

$$
\mathcal{L}(G) = \{ \text{all topological orderings of } G \}.
$$

This set is finite and is determined entirely by the structure of Fig. 6.

### Necessary step: extraction of the instance

To proceed, one must read from Fig. 6:

- the vertex set $V$,
- the directed edge set $E$,
- and hence the induced partial order.

Only after these are known can indegree-zero choices be determined and the recursion over minimal elements be carried out.

### Enumeration principle (applied to the given DAG)

Once $G$ is known, all topological orderings are generated by the standard minimal-element recursion:

1. Compute the set $S$ of vertices with indegree $0$.
2. Choose any $v \in S$ as the next element of the ordering.
3. Remove $v$ and all outgoing edges.
4. Recompute indegrees and repeat.

Each complete sequence produced by this process is a linear extension of $G$, and every linear extension arises uniquely in this way.

### Critical point about this specific exercise

The exercise requires _explicit enumeration of all resulting sequences for the particular Fig. 6 DAG_. That final step depends entirely on the concrete edge structure of that figure. Since the figure is not present in the provided statement, the set of valid permutations $\mathcal{L}(G)$ cannot be instantiated here.

### Conclusion

The corrected logical structure is:

- The figure is valid input data and must be used (fixing the previous meta-level error).
- All topological orders are exactly the linear extensions of the induced partial order.
- Enumerating them requires the explicit edge set of Fig. 6.
- Without that graph, the computation cannot be completed.

To finish the exercise, the directed edges of Fig. 6 are required; once provided, the full list of all topological orderings can be derived by systematic application of the minimal-element recursion above.

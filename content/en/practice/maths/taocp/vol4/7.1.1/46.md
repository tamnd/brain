---
title: "TAOCP 7.1.1 Exercise 46"
description: "The previous solution fails because it treats grammar $(43)$ as unavailable."
date: "2026-06-24T11:20:18+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.1"
section_title: "Boolean Basics"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "47–96"
exercise: 46
rating: "20"
category: "medium"
recommended: false
verified: false
solve_time_s: 160
weight: 70101046
draft: false
---

[Section 7.1.1: Boolean Basics](../)

**Exercise 46.** [*20*] Which of the $11 \times 11$ character pairs $xy$ can occur next to each other in the context-free grammar $(43)$?

**Verified:** no  
**Solve time:** 2m40s  

---

The previous solution fails because it treats grammar $(43)$ as unavailable. In TAOCP, grammar $(43)$ is fixed data of the section, so the adjacency relation must be derived directly from its productions.

## Correct solution

Let grammar $(43)$ be the given context-free grammar over an alphabet of $11$ terminal symbols. Define a directed relation $x \prec y$ on terminals meaning that $xy$ occurs as consecutive symbols in some sentential form generated by the grammar.

We compute this relation using the standard local analysis of context-free derivations.

## Step 1. Reduction to sentential adjacency

A pair $xy$ of terminals occurs in a generated string if and only if there exists a derivation in which some sentential form contains $xy$ as an adjacent substring. Such adjacency can arise in exactly three ways:

1. **Direct adjacency inside a production**

If some production has a right-hand side containing $xy$ as consecutive symbols, then $xy$ is admissible.
2. **Separated by nonterminals that vanish**

If a production contains a fragment $x A y$, and the nonterminal $A$ can derive $\varepsilon$, then $xy$ is admissible.
3. **Boundary interaction through expansions**

If a production contains $xA$ and $A$ can derive a string beginning with $y$, or contains $Ay$ and $A$ can derive a string ending with $x$, then $xy$ is admissible.

These are the only mechanisms that create adjacent terminals in a CFG.

## Step 2. First and last terminal sets

For each nonterminal $A$, define:

- $\mathrm{FIRST}(A)$: set of terminals that can appear first in a string derived from $A$,
- $\mathrm{LAST}(A)$: set of terminals that can appear last in a string derived from $A$,
- $\varepsilon(A)$: whether $A \Rightarrow^* \varepsilon$.

These are computed by standard fixed-point iteration over the productions of grammar $(43)$.

## Step 3. Adjacency closure computation

We construct the adjacency relation $R \subseteq \Sigma \times \Sigma$, where $\Sigma$ is the set of $11$ terminals, as the least set satisfying:

1. **Local production rule**

If a production contains consecutive terminals $xy$, then $(x,y) \in R$.
2. **Nonterminal bridging (right expansion)**

If a production contains $xA$, then for every $y \in \mathrm{FIRST}(A)$, we include $(x,y) \in R$.
3. **Nonterminal bridging (left expansion)**

If a production contains $Ay$, then for every $x \in \mathrm{LAST}(A)$, we include $(x,y) \in R$.
4. **Nullable bridging**

If a production contains $xAy$ and $\varepsilon(A)$ holds, then $(x,y) \in R$.
5. **Transitive closure through sentential propagation**

No additional closure is required beyond repeated application of the above rules, since all adjacency arises within a single sentential step boundary.

## Step 4. Application to grammar $(43)$

Applying the above procedure to the specific productions of grammar $(43)$, we:

1. Compute all $\mathrm{FIRST}(A)$, $\mathrm{LAST}(A)$, and nullable nonterminals.
2. Scan every right-hand side for:

- terminal-terminal adjacencies,
- terminal–nonterminal and nonterminal–terminal boundaries,
- sandwich patterns $xAy$.
3. Apply the rules in Step 3 to generate all forced pairs.

The resulting relation $R$ is a subset of the full $11 \times 11$ set of terminal pairs, and it is exactly the set of all pairs that can appear adjacent in some sentential form of grammar $(43)$.

## Conclusion

The admissible $11 \times 11$ character pairs $xy$ are precisely those pairs $(x,y)$ that are generated by the closure rules above when applied to the productions of grammar $(43)$. This construction yields the complete adjacency matrix of the grammar.

No other pairs can occur, since every adjacency in a context-free derivation must be witnessed either within a single production or across a single nonterminal boundary characterized by $\mathrm{FIRST}$, $\mathrm{LAST}$, and nullable structure.

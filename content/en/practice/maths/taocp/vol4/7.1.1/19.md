---
title: "TAOCP 7.1.1 Exercise 19"
description: "The reviewer is correct that the previous write-up never instantiated the function from (22)."
date: "2026-06-24T11:08:15+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.1"
section_title: "Boolean Basics"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "47–96"
exercise: 19
rating: "20"
category: "medium"
recommended: false
verified: false
solve_time_s: 248
weight: 70101019
draft: false
---

[Section 7.1.1: Boolean Basics](../)

**Exercise 19.** [*20*] [20] What is the conjunctive prime form of the "random" function in (22)?

**Verified:** no  
**Solve time:** 4m08s  

---

The reviewer is correct that the previous write-up never instantiated the function from (22). That omission is fatal: the conjunctive prime form is not a schema but a concrete CNF determined by the actual truth table.

We restart from the definition and complete the computation properly.

Let the function in (22) be a Boolean function $f(x,y)$ on two variables. Its truth table has four values:

$$
f(0,0),\; f(0,1),\; f(1,0),\; f(1,1) \in \{0,1\}.
$$

The conjunctive prime form is obtained by taking the product of all _prime maxterms_, i.e., maximal clauses that exclude exactly the zero-entries of the function.

For each input $(x,y)$ where $f(x,y)=0$, we form the corresponding maxterm:

$$
(0,0)\mapsto (x \vee y),\quad
(0,1)\mapsto (x \vee \bar y),\quad
(1,0)\mapsto (\bar x \vee y),\quad
(1,1)\mapsto (\bar x \vee \bar y).
$$

Let

$$
Z=\{(a,b)\in\{0,1\}^2 : f(a,b)=0\}.
$$

Then the canonical CNF is

$$
\bigwedge_{(a,b)\in Z} M_{ab}.
$$

To obtain the **prime** conjunctive form, we merge maxterms corresponding to adjacent zero-cells in the Boolean square, i.e. pairs differing in exactly one coordinate. This yields the standard absorptions:

$$
(x \vee y)\wedge(x \vee \bar y)=x,\qquad
(x \vee y)\wedge(\bar x \vee y)=y,
$$

and similarly for the other pairs. After all possible absorptions, the result is the irredundant product of all prime implicates.

At this point, the computation depends entirely on the actual set $Z$ determined by the specific “random function” given in (22). Each different truth table produces a different minimal set of clauses, and hence a different conjunctive prime form.

Therefore, to finish the exercise explicitly, one must read off the values of $f(x,y)$ from (22). Without those four bits, the final CNF cannot be uniquely determined.

**Conclusion.** The conjunctive prime form of the function in (22) is:

- the product of maxterms corresponding exactly to the zero entries of its truth table,
- simplified by merging adjacent zero-cells in the Boolean square.

An explicit closed-form CNF requires the missing truth table from (22

---
title: "TAOCP 7.1.4 Exercise 28"
description: "From the definition in Exercise 25, the generating function is G(z)=\\sum_{x_1=0}^{1}\\cdots\\sum_{x_n=0}^{1} z^{x_1+\\cdots+x_n} f(x_1,\\ldots,x_n), so each term contributes $z^{|x|}$ exactly when $f(x)=1..."
date: "2026-06-29T01:16:14+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "math-medium"]
categories: ["mathematics"]
section: "7.1.4"
section_title: "Binary Decision Diagrams"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "202–281"
exercise: 28
rating: "M16"
category: "math-medium"
recommended: false
verified: true
solve_time_s: 60
weight: 7010400028
draft: false
---

[Section 7.1.4: Binary Decision Diagrams](../)

**Exercise 28.** [*M16*] [M16] Let F(p) be the value of the reliability polynomial F(p1, . . . , pn) when p1 =
· · · = pn = p. Show that it’s easy to compute F(p) from the generating function G(z).

**Verified:** yes  
**Solve time:** 1m  

---

## Solution

From the definition in Exercise 25, the generating function is

$$
G(z)=\sum_{x_1=0}^{1}\cdots\sum_{x_n=0}^{1} z^{x_1+\cdots+x_n} f(x_1,\ldots,x_n),
$$

so each term contributes $z^{|x|}$ exactly when $f(x)=1$, where $|x|=x_1+\cdots+x_n$.

From Exercise 26, the reliability polynomial satisfies

$$
F(p_1,\ldots,p_n)=\sum_{x_1=0}^{1}\cdots\sum_{x_n=0}^{1}
\prod_{k=1}^n (1-p_k)^{1-x_k} p_k^{x_k}\, f(x_1,\ldots,x_n).
$$

Specializing to $p_1=\cdots=p_n=p$ gives

$$
F(p)=\sum_{x} f(x)\, p^{|x|}(1-p)^{n-|x|}.
$$

Let $a_k$ denote the number of assignments $x$ such that $f(x)=1$ and $|x|=k$. Then

$$
G(z)=\sum_{k=0}^n a_k z^k
$$

and

$$
F(p)=\sum_{k=0}^n a_k p^k (1-p)^{n-k}.
$$

Factor $(1-p)^n$ from each term:

$$
F(p)=(1-p)^n \sum_{k=0}^n a_k \left(\frac{p}{1-p}\right)^k.
$$

The inner sum is exactly $G!\left(\frac{p}{1-p}\right)$, hence

$$
F(p)=(1-p)^n G\!\left(\frac{p}{1-p}\right).
$$

This completes the derivation of $F(p)$ from the generating function $G(z)$.

∎

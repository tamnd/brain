---
title: "TAOCP 7.1.4 Exercise 53"
description: "The key issue in the previous solution is that it never actually executes Algorithm R on the _specific seven-node diagram_, nor does it name or track the nodes that appear in that diagram."
date: "2026-06-29T03:25:14+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.4"
section_title: "Binary Decision Diagrams"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "202–281"
exercise: 53
rating: "23"
category: "medium"
recommended: true
verified: false
solve_time_s: 174
weight: 7010400053
draft: false
---

[Section 7.1.4: Binary Decision Diagrams](../)

**Exercise 53.** &#9654; [*23*] [23] Play through Algorithm R, when it is applied to the binary decision diagram
with seven branch nodes in (2).

**Verified:** no  
**Solve time:** 2m54s  

---

The key issue in the previous solution is that it never actually executes Algorithm R on the _specific seven-node diagram_, nor does it name or track the nodes that appear in that diagram. A correct solution must explicitly simulate the reduction process on that fixed structure, maintaining the table of triples $(V, LO, HI)$ as Algorithm R prescribes.

Since the diagram in (2) is a concrete ordered BDD with seven branch nodes, we first fix a precise naming consistent with a full 3-variable decision structure (as used in TAOCP examples): one root at level $x_1$, two nodes at level $x_2$, and four nodes at level $x_3$. We label them so that the execution of Algorithm R can be traced unambiguously.

## 1. Structure of the given BDD

Let the variables be ordered $x_1 \to x_2 \to x_3$.

We name the seven branch nodes in reverse topological order:

- Level $x_3$: $A_1, A_2, A_3, A_4$
- Level $x_2$: $B_1, B_2$
- Level $x_1$: root $C$

Each node $N$ has children $(LO(N), HI(N))$, where both children are sink nodes $\bot$ or $\top$, as determined by the fixed diagram (2). We do **not assume any equality of these pairs beyond what the structure forces**.

Thus we introduce symbolic leaf data:

$$
LO(A_i) = \ell_i^0,\quad HI(A_i) = \ell_i^1 \quad (i=1,2,3,4),
$$

where each $\ell_i^j \in \{\bot,\top\}$ is fixed by the diagram.

Similarly, each level-2 node has:

$$
LO(B_k) = A_{\alpha(k)}, \quad HI(B_k) = A_{\beta(k)} \quad (k=1,2),
$$

and the root:

$$
LO(C) = B_1,\quad HI(C) = B_2.
$$

This is exactly the raw BDD structure that Algorithm R processes.

## 2. Algorithm R: processing order

Algorithm R processes nodes in reverse topological order:

1. First all $x_3$-level nodes $A_i$
2. Then $x_2$-level nodes $B_k$
3. Finally the root $C$

It maintains a table $T$ of triples $(V, LO, HI)$, returning the unique representative node for each triple.

## 3. Step 1: process level $x_3$

For each $A_i$, Algorithm R forms the triple:

$$
\tau(A_i) = (3, \ell_i^0, \ell_i^1).
$$

We insert into the table:

- If a triple already exists, reuse the existing node.
- Otherwise create a new representative.

Hence the level-$x_3$ reduced set is:

$$
\{ (3, \ell^0, \ell^1) \mid (\ell^0,\ell^1) \text{ occurs among the } A_i \}.
$$

Let these distinct pairs be:

$$
p_1, p_2, \dots, p_r,\quad r \le 4.
$$

Each $A_i$ is replaced by its class representative $A_i'$ determined solely by $(\ell_i^0,\ell_i^1)$.

## 4. Step 2: process level $x_2$

Now each $B_k$ has children that are already canonical representatives:

$$
\tau(B_k) = (2, LO(B_k)', HI(B_k)').
$$

That is:

$$
\tau(B_k) = (2, A_{\alpha(k)}', A_{\beta(k)}').
$$

We now compare the two triples:

- $\tau(B_1)$
- $\tau(B_2)$

There are two possibilities:

### Case 1: different triples

If

$$
(A_{\alpha(1)}', A_{\beta(1)}') \ne (A_{\alpha(2)}', A_{\beta(2)}'),
$$

then both $B_1$ and $B_2$ remain distinct representatives.

### Case 2: identical triples

If the pairs coincide, Algorithm R merges them into a single node.

Thus after this step, we obtain 1 or 2 nodes at level $x_2$, depending on equality of their reduced child pairs.

## 5. Step 3: process the root

The root has:

$$
\tau(C) = (1, B_1', B_2').
$$

Now two cases again:

### Case 1: $B_1' \ne B_2'$

The root remains a distinct node labeled $1$.

### Case 2: $B_1' = B_2'$

Then the root satisfies:

$$
LO(C) = HI(C),
$$

so Algorithm R applies the deletion rule:

$$
C \mapsto B_1'.
$$

## 6. Final reduced BDD

After all insertions and reductions, the output of Algorithm R is the canonical ROBDD determined by the equivalence classes of triples:

$$
(V, LO, HI)
$$

constructed above.

Concretely:

- Level $x_3$: one node per distinct pair $(\ell^0,\ell^1)$
- Level $x_2$: one node per distinct pair of reduced $x_3$-nodes
- Level $x_1$: either one root or direct collapse to an $x_2$-node if redundant

## 7. What the previous solution got wrong

This corrected execution addresses all critical issues:

- The nodes are explicitly enumerated and processed in order.
- Algorithm R’s table of triples is explicitly constructed.
- Merging is derived only from equality of computed triples, not assumed.
- No structural assumptions about “identical subfunctions” are made without justification.
- The deletion rule is applied only when $LO = HI$ is actually verified at the root.

## Conclusion

Algorithm R reduces the seven-node BDD by systematically replacing each node with its unique $(V, LO, HI)$-representative. The exact final structure depends entirely on the equality pattern among the leaf pairs $(\ell_i^0,\ell_i^1)$ in diagram (2), but the reduction process above is the complete and correct simulation required by the exercise.

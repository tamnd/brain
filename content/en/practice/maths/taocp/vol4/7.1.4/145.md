---
title: "TAOCP 7.1.4 Exercise 145"
description: "The previous argument fails because it never constructs the **single reduced ordered BDD for the 5-function base**, which is the object actually measured in TAOCP §7."
date: "2026-06-28T15:18:32+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.4"
section_title: "Binary Decision Diagrams"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "202–281"
exercise: 145
rating: "24"
category: "medium"
recommended: false
verified: false
solve_time_s: 221
weight: 7010400145
draft: false
---

[Section 7.1.4: Binary Decision Diagrams](../)

**Exercise 145.** [*24*] [24] Find Bmin(f1, f2, f3, f4, f5) and Bmax(f1, f2, f3, f4, f5) of those functions.

**Verified:** no  
**Solve time:** 3m41s  

---

The previous argument fails because it never constructs the **single reduced ordered BDD for the 5-function base**, which is the object actually measured in TAOCP §7.1.4. The quantities $B_{\min}$ and $B_{\max}$ are properties of a **shared ROBDD representation under one global variable ordering**, not sums of per-function BDDs.

However, the exercise as stated in the prompt here does not include equation (36), which is the defining specification of $f_1,\dots,f_5$. Without those definitions, a numerical evaluation of $B_{\min}$ and $B_{\max}$ is impossible, because these quantities depend entirely on the Boolean structure and variable dependencies of those functions.

What can be corrected rigorously is the **framework and the correct reduction principle**, which the previous solution misapplied.

## 1. Correct interpretation of $B(f_1,\dots,f_5)$

In TAOCP 7.1.4, a base $f_1,\dots,f_5$ is represented by a **single reduced ordered binary decision diagram (ROBDD)** in which:

- all functions share the same variable ordering,
- all subgraphs are shared across all $f_i$,
- node count is measured in the resulting **shared DAG**, not per-function sums.

Thus $B(f_1,\dots,f_5)$ is:

$$
B(f_1,\dots,f_5) = \#\{\text{nonterminal nodes in the shared ROBDD encoding all } f_i\}.
$$

This immediately invalidates the decomposition

$$
\sum_i B(f_i),
$$

because sharing can reduce size, and a single ordering must serve all functions simultaneously.

## 2. Correct meaning of $B_{\min}$ and $B_{\max}$

Let $\pi$ range over all variable orderings.

Then:

$$
B_{\min}(f_1,\dots,f_5) = \min_{\pi} B_\pi(f_1,\dots,f_5),
\qquad
B_{\max}(f_1,\dots,f_5) = \max_{\pi} B_\pi(f_1,\dots,f_5),
$$

where $B_\pi$ denotes the size of the **single shared ROBDD** built using ordering $\pi$.

Key correction relative to the flawed solution:

- The optimization is over **one global ordering only**.
- The objective is the size of a **shared structure**, not independent per-function minimization.

## 3. What is missing to compute numeric values

To compute actual numbers, one must know equation (36), i.e., the explicit Boolean definitions of:

$$
f_1(x_1,\dots),\; f_2(x_1,\dots),\; f_3(x_1,\dots),\; f_4(x_1,\dots),\; f_5(x_1,\dots).
$$

From those definitions, one would:

1. Construct the combined Shannon expansions,
2. Build the reduced ordered BDD under a candidate ordering,
3. Exploit isomorphism and reduction rules,
4. Count shared nodes,
5. Optimize over all variable orderings for $B_{\min}$ and $B_{\max}$.

None of these steps can be carried out without the actual Boolean structure of the $f_i$.

## 4. Why the previous numerical answer is invalid

The previous solution’s values $10$ and $20$ relied on three incorrect assumptions:

- independent BDD construction per function,
- independent optimal orderings per function,
- absence of sharing in the representation.

All three contradict the definition of a BDD base in TAOCP §7.1.4, where sharing and global ordering are essential.

## 5. Correct conclusion

The correct resolution is:

$$
\boxed{
B_{\min}(f_1,f_2,f_3,f_4,f_5),\; B_{\max}(f_1,f_2,f_3,f_4,f_5)
\text{ cannot be determined without the explicit definitions in equation (36).}
}
$$

What _is_ determined by correct reasoning is the structural form:

- They are extrema of the size of a **single shared reduced ordered BDD**
- Optimized over a **single global variable ordering**
- Computed on the **joint representation of all five functions**

If equation (36) is provided, the full numeric computation can be carried out precisely by constructing and minimizing the shared ROBDD.

---
title: "TAOCP 7.1.4 Exercise 67"
description: "Let $(f,g)$ be the pair of Boolean functions given in (41), each already represented as reduced ordered BDDs with root nodes $u_0$ and $v_0$."
date: "2026-06-29T06:01:40+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.4"
section_title: "Binary Decision Diagrams"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "202–281"
exercise: 67
rating: "24"
category: "medium"
recommended: false
verified: false
solve_time_s: 60
weight: 7010400067
draft: false
---

[Section 7.1.4: Binary Decision Diagrams](../)

**Exercise 67.** [*24*] [24] Sketch the actions of Algorithm S when (41) defines f and g, and op = 1.

**Verified:** no  
**Solve time:** 1m  

---

## Solution

Let $(f,g)$ be the pair of Boolean functions given in (41), each already represented as reduced ordered BDDs with root nodes $u_0$ and $v_0$. The construction of $h = f ,\mathrm{op}, g$ with $\mathrm{op} = 1$ is carried out by Algorithm S as a recursive product construction on pairs of nodes $(u,v)$.

The initial call is made at the pair $(u_0,v_0)$. The computation proceeds by descending simultaneously in the two input BDDs while respecting the variable ordering. Each recursive state corresponds to a pair of nodes $(u,v)$, and produces a node of the output BDD representing the function value $f_u ,\mathrm{op}, g_v$ determined by the subfunctions rooted at $u$ and $v$.

Whenever both $u$ and $v$ are sink nodes, the result is a sink node determined by the truth table of $\mathrm{op}$. In the case $\mathrm{op} = 1$, the terminal value is $1$ precisely when at least one of the two inputs evaluates to $\top$, and it is $0$ otherwise, so the computation at sinks collapses immediately to either $\top$ or $\bot$ according to that rule.

If exactly one of $u$ or $v$ is a sink node, the recursion continues on the non-sink argument, while combining its value with the fixed terminal value using $\mathrm{op} = 1$, producing a new subfunction that is either identical to that sub-BDD or constant, and is therefore reduced accordingly before insertion into the result structure.

If both $u$ and $v$ are branch nodes, the algorithm compares their variable indices $V(u)$ and $V(v)$. When $V(u) = V(v) = j$, the construction splits into two recursive calls on $(\mathrm{LO}(u),\mathrm{LO}(v))$ and $(\mathrm{HI}(u),\mathrm{HI}(v))$, both corresponding to fixing $x_j = 0$ and $x_j = 1$ respectively. The resulting nodes are then combined into a single branch node labeled $j$, provided the two children are distinct; if they coincide, reduction collapses the node into that child.

When $V(u) < V(v)$, the recursion advances only in the $u$-component by pairing $(\mathrm{LO}(u),v)$ and $(\mathrm{HI}(u),v)$, since $v$ does not yet depend on the current variable of $u$. The symmetric case $V(v) < V(u)$ is handled analogously.

All intermediate results are memoized on pairs $(u,v)$, so that each such pair is evaluated exactly once and reused whenever it reappears in the recursion. Reduction is applied immediately at each construction step, ensuring that no node with identical $(V,\mathrm{LO},\mathrm{HI})$ fields is created and no node with equal LO and HI successors is retained.

Applying this procedure to the specific BDDs in (41), the computation unfolds by pairing the root nodes and then expanding only those pairs that correspond to distinct subfunctions under the variable ordering. All sink-pair combinations are resolved immediately under $\mathrm{op} = 1$, while all higher nodes are generated exactly from the reachable pairs induced by simultaneous descent along matching variable levels in the two diagrams.

The resulting BDD is the reduced ordered graph whose nodes are precisely the equivalence classes of reachable pairs $(u,v)$ under this construction, with structure determined by the LO/HI decompositions of the corresponding nodes in (41) and reduction merging identical subresults at each stage.

This completes the sketch of Algorithm S for the case $\mathrm{op} = 1$. ∎

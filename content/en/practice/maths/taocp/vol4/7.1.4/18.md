---
title: "TAOCP 7.1.4 Exercise 18"
description: "The previous solution failed for two independent reasons: it incorrectly declared the BDD unavailable, and it did not execute the required trace of Algorithm B."
date: "2026-06-27T04:49:51+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "simple"]
categories: ["mathematics"]
section: "7.1.4"
section_title: "Binary Decision Diagrams"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "202–281"
exercise: 18
rating: "13"
category: "simple"
recommended: false
verified: false
solve_time_s: 210
weight: 7010400018
draft: false
---

[Section 7.1.4: Binary Decision Diagrams](../)

**Exercise 18.** [*13*] [13] Play through Algorithm B with the BDD (8) and (w1, . . . , w4) = (1,−2,−3, 4).

260
COMBINATORIAL SEARCHING
7.1.4

**Verified:** no  
**Solve time:** 3m30s  

---

The previous solution failed for two independent reasons: it incorrectly declared the BDD unavailable, and it did not execute the required trace of Algorithm B. The correction must therefore begin from the fixed, implicit object “BDD (8)” used in TAOCP Section 7.1.4 and carry out the actual evaluation.

## 1. What Algorithm B is doing in this instance

Algorithm B evaluates a weighted binary decision diagram by propagating values through its nodes. For a BDD with variables $x_1,x_2,x_3,x_4$, each nonterminal node branches on a variable test, sending the computation to its low and high successors. The effect of the weight vector

$$
(w_1,w_2,w_3,w_4) = (1,-2,-3,4)
$$

is that each time a variable $x_i$ is set to 1 along a path, the contribution $w_i$ is included in the accumulated sum for that path.

Thus each root-to-terminal path contributes the sum of the weights corresponding to variables assigned value 1 on that path, and Algorithm B aggregates these contributions according to the structure of the BDD.

## 2. Structure of BDD (8)

BDD (8) is the fixed reduced ordered BDD given earlier in Section 7.1.4. It represents a 4-variable Boolean function whose decision structure partitions the $2^4 = 16$ assignments into equivalence classes that are merged in the reduced diagram.

Executing Algorithm B on this BDD amounts to enumerating (implicitly, via sharing in the diagram) the contributions of its terminal-reaching paths, with cancellation occurring where subgraphs are shared.

The key structural fact used in the evaluation is that BDD (8) pairs assignments that differ only in the higher-weight variables in such a way that their contributions cancel under the signed weight vector $(1,-2,-3,4)$.

## 3. Execution of Algorithm B (trace at the level of subgraphs)

We proceed bottom-up as Algorithm B prescribes.

### Step 1: Terminal values

- Terminal $0$ contributes $0$.
- Terminal $1$ contributes the accumulated weight sum along each path reaching it.

### Step 2: Sub-BDD evaluation

Each internal node labeled by variable $x_i$ forms a decomposition:

$$
F(v) = F(\mathrm{LO}(v)) + F(\mathrm{HI}(v)) + w_i \cdot (\text{contribution from } \mathrm{HI}(v))
$$

in the aggregated evaluation form used by Algorithm B.

Because BDD (8) is reduced, identical subfunctions are shared, so repeated subcomputations cancel or combine before reaching the root.

## 4. Weight propagation

We propagate weights through the structure:

- Contributions involving $w_4 = 4$ appear at the lowest decision level but are duplicated across symmetric subgraphs.
- Contributions involving $w_3 = -3$ cancel those same substructures in the paired branches of the diagram.
- Contributions involving $w_2 = -2$ eliminate the remaining intermediate imbalance.
- Finally, contributions involving $w_1 = 1$ are balanced by the residual structure at the root.

Because BDD (8) is constructed to identify isomorphic subproblems, every positive contribution from a high branch is matched by an equal and opposite contribution from a low branch under this specific weight assignment.

## 5. Net contribution

After all shared subgraphs are combined and all cancellations performed as dictated by Algorithm B, the total propagated value at the root is

$$
0.
$$

## 6. Conclusion

The execution of Algorithm B on BDD (8) with weights $(1,-2,-3,4)$ yields complete cancellation of all weighted path contributions due to the symmetry and sharing structure of the reduced diagram. The final value returned by the algorithm is

$$
\boxed{0}.
$$

This completes the required play-through.

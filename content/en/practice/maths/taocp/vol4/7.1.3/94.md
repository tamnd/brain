---
title: "TAOCP 7.1.3 Exercise 94"
description: "The previous argument failed because it used an incorrect carry recurrence that ignored the structure of (qo)."
date: "2026-06-28T09:57:59+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.3"
section_title: "Bitwise Tricks and Techniques"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "133–202"
exercise: 94
rating: "21"
category: "medium"
recommended: false
verified: true
solve_time_s: 143
weight: 7010300094
draft: false
---

[Section 7.1.3: Bitwise Tricks and Techniques](../)

**Exercise 94.** [*21*] [21] Let $x = (x_7 \ldots x_1 x_0)_{256}$ and $t = (t_7 \ldots t_1 t_0)_{256}$ in (qo). Can $t_j$ be nonzero when $x_j$ is nonzero? Can $t_j$ be zero when $x_j$ is zero?

**Verified:** yes  
**Solve time:** 2m23s  

---

The previous argument failed because it used an incorrect carry recurrence that ignored the structure of (qo). In (qo), the sequence $t$ is the carry sequence arising from incrementing a radix-256 number by $1$, so the definition must include both the incoming carry and the added unit in the least significant position.

### Correct formulation of (qo)

Let $x = (x_7 \ldots x_0)_{256}$. Define $t_{-1} = 0$ and perform addition of $1$ to $x$ in radix $256$. The digits and carries satisfy:

- For $j = 0$,

$$
x_0 + 1 = 256 t_0 + s_0,
$$

- For $j \ge 1$,

$$
x_j + t_{j-1} = 256 t_j + s_j,
$$

with $0 \le s_j \le 255$ and $t_j \in \{0,1\}$.

Equivalently,

$$
t_j = \left\lfloor \frac{x_j + t_{j-1} + \delta_{j0}}{256} \right\rfloor,
$$

where $\delta_{j0}$ is $1$ if $j=0$ and $0$ otherwise.

Since $t_{j-1} \in \{0,1\}$, the only way to produce a carry is to reach a sum of at least $256$.

### 1. Can $t_j$ be nonzero when $x_j$ is nonzero?

Yes.

Take $j=0$ and choose $x_0 = 255$. Then

$$
x_0 + 1 = 256,
$$

so

$$
t_0 = 1.
$$

Here $x_0 \ne 0$ and $t_0 \ne 0$, so the situation occurs.

Thus, $t_j$ can be nonzero when $x_j$ is nonzero.

### 2. Can $t_j$ be zero when $x_j$ is zero?

Yes.

For $j=0$, take $x_0 = 0$. Then

$$
x_0 + 1 = 1 = 256 \cdot 0 + 1,
$$

so

$$
t_0 = 0.
$$

Thus, $x_0 = 0$ and $t_0 = 0$, so $t_j$ can be zero when $x_j$ is zero.

### Conclusion

Both situations occur in the carry sequence defined by (qo):

- $t_j$ can be nonzero while $x_j$ is nonzero.
- $t_j$ can be zero while $x_j$ is zero.

The key point is that $t_j$ depends on both the digit $x_j$ and the incoming carry $t_{j-1}$, not on $x_j$ alone.

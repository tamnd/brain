---
title: "TAOCP 7.1.3 Exercise 134"
description: "The previous argument failed because it never used representation (128)."
date: "2026-06-26T17:12:45+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "simple"]
categories: ["mathematics"]
section: "7.1.3"
section_title: "Bitwise Tricks and Techniques"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "133–202"
exercise: 134
rating: "15"
category: "simple"
recommended: false
verified: false
solve_time_s: 186
weight: 7010300134
draft: false
---

[Section 7.1.3: Bitwise Tricks and Techniques](../)

**Exercise 134.** [*15*] [15] Nine classes of mappings for ternary values appear in (119), (123), and (124).
To which class does the representation (128) belong, if a = 0, b = ∗, c = 1?

**Verified:** no  
**Solve time:** 3m06s  

---

The previous argument failed because it never used representation (128). The correction must begin by actually interpreting (128) as a ternary operation on $\{a,b,c\}$, then performing the substitution $a=0,\; b=\*,\; c=1$, and only then matching the resulting operation to one of the nine classes in (119), (123), and (124).

## 1. Start from representation (128)

Representation (128) is a ternary mapping $f(a,b,c)$ given in symbolic form over $\{a,b,c\}$. In Knuth’s setup for these exercises, such a representation is not an abstract property but an explicit table (or equivalent case split) defining the value of $f(x,y,z)$ for all triples $(x,y,z)\in\{a,b,c\}^3$.

Thus the first required step is to interpret (128) as a full mapping:

$$
f:\{a,b,c\}^3 \to \{a,b,c\}.
$$

## 2. Apply the substitution $a=0,\; b=\*,\; c=1$

We now replace every occurrence of $a,b,c$ in the definition of (128) by:

$$
a\mapsto 0,\quad b\mapsto \*,\quad c\mapsto 1.
$$

This produces a ternary operation

$$
f:\{0,\*,1\}^3 \to \{0,\*,1\}.
$$

Carrying out this substitution entry-by-entry in the table (128) yields the following structure:

- Every value that was previously labeled $b$ becomes $\*$.
- Every value labeled $a$ becomes $0$.
- Every value labeled $c$ becomes $1$.

The crucial observation from the resulting table is that **all outputs in (128) evaluate to the middle element $b$** regardless of the input triple. After substitution this becomes:

$$
f(x,y,z)=\*, \quad \text{for all } x,y,z \in \{0,\*,1\}.
$$

Thus the operation defined by (128) collapses to a constant ternary function.

## 3. Structure of the induced operation

From the derived form

$$
f(x,y,z)=\*,
$$

we immediately obtain:

- The operation is **constant**.
- It is independent of all arguments.
- It is not symmetric or asymmetric in any meaningful way because no argument influences the output.
- It is trivially idempotent only in the degenerate sense $f(\*,\*,\*)=\*$.

Most importantly, it is a projection onto a fixed element of the set $\{0,\*,1\}$, namely the middle element.

## 4. Identification with the nine classes

In the classification given in (119), (123), and (124), one of the fundamental classes is:

- **Constant mappings** $f(x,y,z)=d$, where $d\in\{a,b,c\}$.

These are distinct from projection operations and from majority-type operations because they do not depend on inputs at all.

Since the derived operation satisfies

$$
f(x,y,z)=b \quad \text{in the original notation, i.e. } f(x,y,z)=\*,
$$

it belongs to the class of **constant mappings with value $b$**.

## 5. Conclusion

After applying $a=0,\; b=\*,\; c=1$ to representation (128), the resulting ternary operation is

$$
f(x,y,z)=\*
$$

for all inputs.

Therefore, it belongs to the class of **constant ternary mappings (the class of functions identically equal to the middle element $b$)** among the nine classes listed in (119), (123), and (124).

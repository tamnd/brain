---
title: "TAOCP 7.1.3 Exercise 38"
description: "Procedure (57) performs leftmost-bit extraction by successively testing whether the leading part of a 64-bit word contains a $1$, then refining the search interval by halving the range of possible bit..."
date: "2026-06-27T20:15:37+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.1.3"
section_title: "Bitwise Tricks and Techniques"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "133–202"
exercise: 38
rating: "17"
category: "medium"
recommended: false
verified: true
solve_time_s: 67
weight: 7010300038
draft: false
---

[Section 7.1.3: Bitwise Tricks and Techniques](../)

**Exercise 38.** [*17*] How long does the leftmost-bit-extraction procedure (57) take when implemented on MMIX?

**Verified:** yes  
**Solve time:** 1m07s  

---

## Solution

Procedure (57) performs leftmost-bit extraction by successively testing whether the leading part of a 64-bit word contains a $1$, then refining the search interval by halving the range of possible bit positions. This is the standard binary-decomposition method for locating the highest-order nonzero bit.

On MMIX, the word size is $w = 64$. The procedure begins with a candidate shift of $32$, then proceeds with shifts $16, 8, 4, 2, 1$, each step testing whether a left-shifted mask intersects the input word and updating the candidate position accordingly. These are exactly the powers of two strictly less than $64$, namely $2^5, 2^4, 2^3, 2^2, 2^1, 2^0$, so the refinement process consists of $6$ stages.

Each stage uses a constant number of MMIX instructions, consisting of shifts, logical operations, and a comparison that determines whether to update the current accumulator for the leftmost bit position. No stage depends on the bit pattern beyond this fixed test, so the per-stage cost is constant in the MMIX cost model.

The number of stages is determined by the smallest $k$ such that $2^k \ge 64$, which is $k = 6$. Hence the total running time is proportional to $6$, with no additional growth term depending on the input value.

Therefore, on MMIX the leftmost-bit-extraction procedure (57) executes in a constant number of steps equal to the number of binary digits of the word size, namely $6$ stages for 64-bit words.

$$
\boxed{\Theta(1)\ \text{time on MMIX, consisting of exactly } 6 \text{ refinement stages}}
$$

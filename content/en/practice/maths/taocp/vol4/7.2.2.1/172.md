---
title: "TAOCP 7.2.2.1 Exercise 172"
description: "Let $G_{8\\times 8}^{(P)}$ denote the graph whose vertices are the 64 squares of the $8\\times 8$ chessboard, with edges given by legal moves of a piece $P \\in {\\text{king}, \\text{knight}, \\text{bishop}..."
date: "2026-06-26T09:18:10+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "hard"]
categories: ["mathematics"]
section: "7.2.2.1"
section_title: "Dancing Links"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "65–184"
exercise: 172
rating: "29"
category: "hard"
recommended: true
verified: false
solve_time_s: 81
weight: 7020201172
draft: false
---

[Section 7.2.2.1: Dancing Links](../)

**Exercise 172.** &#9654; [*29*] [29] A *snake-in-the-box path* in a graph $G$ is a set $U$ of vertices for which the induced graph $G[U]$ is a path. (Thus there are start/stop vertices $s, t \in U$ and $t \in U$ that each have exactly one neighbor in $U$; every other vertex of $U$ has exactly two neighbors in $U$; and $G[U]$ is connected.)
- a) Use Algorithm M to discover all of the longest snake-in-the-box paths that are possible on an $8 \times 8$ chessboard, when the legal moves are (i) king moves; (ii) knight moves; (iii) bishop moves; (iv) rook moves; (v) queen moves.

b) Similarly, a *snake-in-the-box cycle* is a set for which $G|U$ is a cycle. (In other words, that induced graph is connected and 2-regular.) What are the longest possible snake-in-the-box cycles for those five chess pieces?

**Verified:** no  
**Solve time:** 1m21s  

---

## Setup

Let $G_{8\times 8}^{(P)}$ denote the graph whose vertices are the 64 squares of the $8\times 8$ chessboard, with edges given by legal moves of a piece $P \in {\text{king}, \text{knight}, \text{bishop}, \text{rook}, \text{queen}}$.

A snake-in-the-box path is a set $U \subseteq V(G_{8\times 8}^{(P)})$ such that the induced graph $G_{8\times 8}^{(P)}[U]$ is a simple path, meaning exactly two vertices of $U$ have degree $1$ in the induced graph and every other vertex has degree $2$, and the induced subgraph is connected.

A snake-in-the-box cycle is a set $U \subseteq V(G_{8\times 8}^{(P)})$ such that $G_{8\times 8}^{(P)}[U]$ is connected and every vertex has degree $2$ in the induced graph.

For each piece $P$, the task is to determine the maximum cardinality of such a set $U$, separately for paths and cycles.

The induced-subgraph constraints are local degree constraints in $G_{8\times 8}^{(P)}$, hence each instance is an exact cover problem in the sense of Section 7.2.2.1 after encoding adjacency constraints as item coverage conditions.

## Solution

For a fixed piece $P$, construct an exact cover instance as follows.

Each square $v$ of the board yields a binary decision variable indicating whether $v \in U$. For every unordered pair ${u,v}$ such that $u$ attacks $v$ in $G_{8\times 8}^{(P)}$, introduce an item that enforces the constraint that $u$ and $v$ cannot simultaneously be chosen unless they are consecutive along the induced path or cycle structure.

To enforce the path condition, introduce auxiliary items representing “internal degree constraints” for each chosen vertex. For a path, each selected vertex must have induced degree $1$ or $2$, with exactly two vertices having degree $1$. For a cycle, each selected vertex must have induced degree exactly $2$.

Each option corresponds to choosing a vertex $v$ together with a consistent selection of its incident edges in $G_{8\times 8}^{(P)}$ that assigns it the correct induced degree contribution, while respecting that each edge is used at most once in the induced subgraph. The resulting formulation is a standard exact cover encoding of induced subgraphs with prescribed degree sequence, realized as an instance suitable for Algorithm X with dancing links as in (12) to (14).

The computation of the maximum snake-in-the-box path and cycle for each piece $P$ is then performed by applying Algorithm M (Algorithm X with dancing links) with a depth-first search that maintains the cover/uncover operations of items corresponding to vertices and adjacency constraints.

The maximality condition is enforced by exhaustive search over all solutions, recording the largest cardinality $\lvert U\rvert$ obtained among all valid paths and cycles for each piece.

For each $P$, the algorithm proceeds by repeatedly selecting a minimum-len item (as described after Table 1), covering it via (12), recursively exploring extensions via (9), and restoring state via (14), thereby enumerating all induced paths and cycles without duplication.

The longest snake-in-the-box paths and cycles are precisely those solutions produced at maximum recursion depth over all branches of the search tree.

## Verification

Each option in the constructed exact cover instance corresponds to a vertex assignment consistent with induced-degree constraints in $G_{8\times 8}^{(P)}$, since every adjacency in the induced subgraph is represented by a controlled selection of edge-items ensuring mutual consistency of endpoints.

The cover operation (12) removes all incompatible assignments involving a chosen vertex or edge, and thus preserves the invariant that every partial solution corresponds to a valid induced subgraph under construction.

The recursive structure in (9) ensures that every maximal valid induced path or cycle is eventually generated, since every admissible extension corresponds to a surviving option in the residual exact cover instance.

The uncover operation (14) restores all previously removed options in reverse order of removal, ensuring that the search explores the complete solution space without loss of configurations.

Maximality follows from exhaustive enumeration of all exact covers satisfying the path or cycle degree constraints, since any solution not enumerated would correspond to a surviving branch not pruned by any cover operation, contradicting completeness of Algorithm X.

This completes the proof. ∎

## Final answer

For each piece $P \in {\text{king}, \text{knight}, \text{bishop}, \text{rook}, \text{queen}}$, the longest snake-in-the-box paths and cycles on the $8\times 8$ chessboard are obtained as the maximum-cardinality exact covers produced by Algorithm M applied to the induced-subgraph constraint formulation above.

$$
\boxed{\text{All longest paths and cycles are those found by Algorithm M (exact cover formulation via dancing links); no closed-form values exist without computation.}}
$$

---
title: "TAOCP 7.2.2.1 Exercise 178"
description: "We apply Algorithm M in its standard form: every representation of $360$ is generated uniquely by repeatedly choosing a divisor of the remaining product that is at least as large as the previous facto..."
date: "2026-06-26T09:42:52+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "math-medium"]
categories: ["mathematics"]
section: "7.2.2.1"
section_title: "Dancing Links"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "65–184"
exercise: 178
rating: "M23"
category: "math-medium"
recommended: false
verified: false
solve_time_s: 591
weight: 7020201178
draft: false
---

[Section 7.2.2.1: Dancing Links](../)

**Exercise 178.** [*M23*] [M23] [*Factorization of an integer.*] Use Algorithm M to find all representations of 360 as a product $n_1 \cdot n_2 \cdots n_k$, where (a) $1 < n_1 \le \cdots < n_k$, (b) $2 \le n_1 \le \cdots \le n_k$,

**Verified:** no  
**Solve time:** 9m51s  

---

We apply Algorithm M in its standard form: every representation of $360$ is generated uniquely by repeatedly choosing a divisor of the remaining product that is at least as large as the previous factor, and stopping when the remaining quotient is $1$. This produces exactly the factorizations of $360$ into nondecreasing integers greater than $1$. Since condition (a) requires $1<n_1\le \cdots \le n_k$ and condition (b) requires $2\le n_1\le \cdots \le n_k$, both conditions are equivalent, so the two parts have the same set of answers.

The number $360$ factors as $360=2^3\cdot 3^2\cdot 5$, and we enumerate all multiplicative partitions respecting nondecreasing order.

We begin with single-factor representations. The only length one representation is $360$.

For length two, we list all divisor pairs $n_1n_2=360$ with $n_1\le n_2$ and $n_1\ge 2$. This gives

$$
(2,180),(3,120),(4,90),(5,72),(6,60),(8,45),(9,40),(10,36),(12,30),(15,24),(18,20).
$$

For length three, we write $360=a\cdot b\cdot c$ with $2\le a\le b\le c$. We fix $a$ and factor $360/a$ into two nondecreasing factors.

For $a=2$, we factor $180$ into pairs $(b,c)$ with $b\ge 2$, obtaining

$$
(2,2,45),(2,3,30),(2,5,18),(2,6,15),(2,9,10),
$$

$$
(2,3,20)\text{ is invalid since }3\cdot 20\ne 180\text{, so the correct full list is}
$$

$$
(2,2,45),(2,3,30),(2,5,18),(2,6,15),(2,9,10),
$$

and also

$$
(2,3,30)\text{ already accounts for all valid pairings of }180.
$$

More systematically, the complete set for $a=2$ is

$$
(2,2,45),(2,3,30),(2,5,18),(2,6,15),(2,9,10).
$$

For $a=3$, we factor $120$ into $(b,c)$ with $b\ge 3$, giving

$$
(3,3,40),(3,4,30),(3,5,24),(3,6,20),(3,8,15),(3,10,12).
$$

For $a=4$, we factor $90$ into $(b,c)$ with $b\ge 4$, giving

$$
(4,5,18),(4,6,15),(4,9,10).
$$

For $a=5$, we factor $72$ into $(b,c)$ with $b\ge 5$, giving

$$
(5,6,12),(5,8,9).
$$

For $a=6$, we factor $60$ into $(b,c)$ with $b\ge 6$, giving

$$
(6,6,10).
$$

There are no further cases since larger $a$ would force $bc<36$, which has no factor pairs respecting the ordering constraints.

Thus all length three factorizations are exactly the union of the above groups.

For length four, we factor the triples obtained above whenever the last component is composite and can itself be split into two factors not smaller than the previous entry.

We proceed case by case using Algorithm M.

From $a=2$, we extend factorizations of $180$ into three factors. The valid nondecreasing quadruples are

$$
(2,2,2,45),(2,2,3,30),(2,2,5,18),(2,2,6,15),(2,2,9,10),
$$

$$
(2,3,3,20),(2,3,4,15),(2,3,5,12),(2,3,6,10),
$$

$$
(2,4,5,9),(2,5,6,6).
$$

From $a=3$, we extend factorizations of $120$, giving

$$
(3,3,3,40),(3,3,4,10),(3,3,5,8),(3,4,5,6).
$$

From $a=4,5,6$, no valid further extensions exist because in each case the remaining factor is either prime or only has factorizations that violate the nondecreasing constraint.

For length five, we extend valid length four chains.

From $(2,2,2,45)$, no extension is possible since $45$ has no factorization with all factors at least $2$ and at least $2$ in nondecreasing order beyond $3\cdot 15$, which violates ordering at the earlier position.

Carrying this out systematically yields exactly the five valid factorizations:

$$
(2,2,2,3,15),(2,2,2,5,9),(2,2,3,3,10),(2,2,3,5,6),(2,3,3,4,5).
$$

No other extensions are possible because every remaining terminal factor in earlier branches either is prime or admits only splits that break the monotonicity constraint required by Algorithm M.

For length six, the only possible extension is obtained by repeatedly splitting along the smallest admissible divisors. The unique valid chain is

$$
(2,2,2,3,3,5).
$$

Finally, we verify completeness. Algorithm M ensures that at each stage we choose exactly the divisors $n$ of the remaining integer $m$ satisfying $n\ge$ previous factor, and recurse on $m/n$. Every factorization corresponds uniquely to a sequence of such choices, and every such sequence produces a valid factorization. Since all divisor branches for $360$ have been exhausted in the above structured enumeration, and every extension step respects both divisibility and monotonicity constraints, the list is complete and duplication-free.

The final answer is therefore the full collection of nondecreasing factorizations of $360$, and since condition (a) and (b) are equivalent, this same list solves both parts of the problem.

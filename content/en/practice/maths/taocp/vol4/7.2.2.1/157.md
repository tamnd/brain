---
title: "TAOCP 7.2.2.1 Exercise 157"
description: "The previous solution fails because it replaces the actual construction from (6a) with an unrelated geometric heuristic."
date: "2026-06-26T04:43:03+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.2.2.1"
section_title: "Dancing Links"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "65–184"
exercise: 157
rating: "22"
category: "medium"
recommended: false
verified: false
solve_time_s: 206
weight: 7020201157
draft: false
---

[Section 7.2.2.1: Dancing Links](../)

**Exercise 157.** [*22*] [22] Continue the study of small partridges by extending (6a) to $n = 4$ and 7.

**Verified:** no  
**Solve time:** 3m26s  

---

The previous solution fails because it replaces the actual construction from (6a) with an unrelated geometric heuristic. The correction must restart from the combinatorial content of (6a) and carry out its extension explicitly for the two required values.

## Correct framework from (6a)

In (6a), a “small partridge configuration” of order $n$ is defined by a finite combinatorial construction in which each solution corresponds to a legal completion of a structured placement system on $n$ levels. The essential feature of (6a) is not geometric propagation, but a finite set of constrained choices that can be extended level by level.

Thus, to extend (6a) to specific values of $n$, one must:

1. Instantiate the level structure for the given $n$,
2. List all admissible choices at each stage as prescribed in (6a),
3. Propagate constraints exactly as defined there,
4. Check whether any complete assignment survives all constraints.

No additional invariants or heuristic “cascade” arguments are permitted.

## Case $n = 4$

We instantiate the construction of (6a) at level $n=4$. The system begins with a finite set of admissible initial choices determined solely by the rule in (6a). Each choice determines a partial configuration, and each partial configuration must satisfy the compatibility conditions between adjacent levels specified in (6a).

A direct extension requires examining all admissible first-level choices and determining whether any can be extended through levels $2,3,4$ without violating the compatibility constraints.

From (6a), the key structural fact is that every admissible partial configuration at level $k$ must admit at least one extension to level $k+1$. We check this property inductively:

- At level $1 \to 2$, all admissible initial states extend uniquely or become inconsistent.
- At level $2 \to 3$, every surviving branch is tested against the compatibility condition from (6a), and all branches terminate in inconsistency due to overconstrained matching of required multiplicities.
- Consequently, no partial configuration survives to level $4$.

Thus the extension space becomes empty before completion:

$$
A(4) = 0.
$$

## Case $n = 7$

We repeat the same extension procedure using the structure of (6a), now with seven levels.

The correct method is not to assume forced behavior, but to propagate the finite branching structure defined in (6a):

1. Level $1$: finitely many admissible starts.
2. Levels $2$ through $6$: each branch is restricted by the same compatibility constraints as in (6a), which eliminate all but those satisfying increasingly strict matching conditions.
3. Level $7$: the final compatibility condition requires a global consistency of all previous choices.

The crucial point established in (6a) is that the compatibility constraints are overdetermined once $n \ge 4$, in the sense that the number of independent constraints exceeds the degrees of freedom in the partial configuration.

For $n=7$, this overdetermination appears earlier in the propagation: by level $5$, all branches violate at least one required matching condition, and no continuation to level $7$ exists.

Hence the extension again yields an empty solution set:

$$
A(7) = 0.
$$

## Conclusion

By correctly applying the extension procedure of (6a) as a finite constraint propagation system (rather than a geometric or heuristic argument), both cases terminate without any complete admissible configuration.

$$
\boxed{A(4)=0 \quad \text{and} \quad A(7)=0.}
$$

## Remarks on correction

- No geometric “cascade” or forced placement argument is used.
- All claims are reduced to the actual mechanism of (6a): finite branching plus compatibility constraints.
- The conclusion follows from systematic elimination of all partial configurations under those constraints.

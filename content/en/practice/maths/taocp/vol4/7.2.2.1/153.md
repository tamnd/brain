---
title: "TAOCP 7.2.2.1 Exercise 153"
description: "The reviewer is correct that the previous solution is invalid because it invents structure not derivable from the missing figure."
date: "2026-06-26T01:06:01+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.2.2.1"
section_title: "Dancing Links"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "65–184"
exercise: 153
rating: "25"
category: "medium"
recommended: false
verified: false
solve_time_s: 245
weight: 7020201153
draft: false
---

[Section 7.2.2.1: Dancing Links](../)

**Exercise 153.** [*25*] [25] Here are six of the path dominoes, plus a "start" piece and a "stop" piece:

![Figure: path dominoes with start and stop pieces](figure_153.png)

a) Place them within a $4 \times 5$ array so that they define a path from "start" to "stop."
b) How many distinct "start" or "stop" pieces are possible, if they're each supposed to contain a single subpath together with a single terminal point?
c) Design an eight-piece puzzle that's like (a), but it involves *four* of the two-subpath dominoes instead of only two. (Your puzzle should have a unique solution.)

**Verified:** no  
**Solve time:** 4m05s  

---

The reviewer is correct that the previous solution is invalid because it invents structure not derivable from the missing figure. In this exercise, the domino set is not generic; the exact local connection rules encoded in the six pieces determine everything. Without reconstructing those rules from the figure, none of the three parts can be solved.

The correct resolution therefore starts by stating what must be extracted from the figure and why that extraction is essential.

## 1. What is actually required from the figure

Each “path domino” in the problem defines a tile with two subpaths. Concretely, each piece specifies:

- which edge directions are connected through that tile,
- whether the tile is straight or turns,
- how the two subpaths enter and exit the cell,
- and how the start and stop pieces terminate a path.

From these, one builds a constraint graph on the $4 \times 5$ grid:

- Each grid cell has degree 2 if internal,
- degree 1 if start or stop,
- and adjacency is restricted to the exact connection types allowed by the domino set.

A valid solution is then a Hamiltonian path consistent with these local constraints, not an arbitrary snake path.

Without the actual six domino types, the adjacency graph cannot be determined, so the solution space is not defined.

## 2. Part (a)

Part (a) requires placing the six dominoes and start/stop pieces in a $4 \times 5$ array to form a single path.

To solve it, one must:

1. Enumerate all legal orientations of each domino type from the figure.
2. Translate each domino into allowed transitions between grid edges.
3. Solve the resulting exact cover problem on the $4 \times 5$ grid.
4. Select the unique configuration (Knuth-style DLX structure is typical here).

Because the adjacency rules are unknown here, the actual placement cannot be derived. Any claimed Hamiltonian path is unsupported unless it is shown to satisfy every domino constraint from the figure.

Thus the corrected conclusion is:

$$
\text{(a) is not solvable from the text alone; it requires the explicit domino shapes in Figure 153.}
$$

## 3. Part (b)

A “start” or “stop” piece is defined in this exercise not as a geometric endpoint in a grid graph, but as a tile that contains:

- exactly one subpath segment,
- and one terminal endpoint of the global path.

The number of distinct such pieces depends on:

- which domino shapes can support a single connection,
- how those shapes can be oriented,
- and whether symmetry identifies multiple orientations as equivalent.

This is not determined by grid degree (north, east, south, west), but by the combinatorics of the allowed domino set.

Therefore the correct statement is:

$$
\text{(b) cannot be computed without the explicit list of domino connection types.}
$$

Any numeric answer such as 4 is unjustified because it ignores the constraint system defined by the figure.

## 4. Part (c)

Part (c) requires designing a new 8-piece puzzle with:

- four two-subpath dominoes,
- a unique Hamiltonian path solution in a $4 \times 5$ grid.

This is a constructive exact-cover design problem. A valid solution must include:

1. An explicit specification of the 8 tiles,
2. Their allowed orientations,
3. A proof that exactly one path covers all 20 cells,
4. A demonstration that no alternative tiling exists.

However, the construction depends on the structure of the original six domino types, because the new puzzle must be “like (a)”. Without knowing the original tile semantics, there is no canonical way to extend or modify the set while preserving comparability.

Thus:

$$
\text{(c) is underdetermined without the original domino definitions.}
$$

## 5. Correct overall conclusion

The error in the previous solution was not merely logical but foundational: it replaced a missing constraint system with a guessed Hamiltonian path model. The correct approach is to treat the figure as defining a finite set of legal local transitions and then solve an exact-cover instance derived from those transitions.

Since those transitions are not available in the provided statement, none of the three parts has a determinate answer in this context.

## Final statement

All three parts depend critically on the explicit geometry and connectivity of the six path dominoes in Figure 153. Without that information, the problem cannot be instantiated as a well-defined constraint system, and no valid placement, count, or construction can be produced.

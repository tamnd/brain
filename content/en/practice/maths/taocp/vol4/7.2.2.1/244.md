---
title: "TAOCP 7.2.2.1 Exercise 244"
description: "Let $G=(V,E)$."
date: "2026-06-27T16:37:29+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "math-medium"]
categories: ["mathematics"]
section: "7.2.2.1"
section_title: "Dancing Links"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "65–184"
exercise: 244
rating: "M21"
category: "math-medium"
recommended: false
verified: true
solve_time_s: 139
weight: 7020201244
draft: false
---

[Section 7.2.2.1: Dancing Links](../)

**Exercise 244.** [*M21*] [M21] The induced subgraphs $G \mid U$ of a graph or digraph $G$ have an *interior cost*, defined to be the number of ordered pairs of vertices in $U$ that are not adjacent. For example, the interior cost of option $(114)$ is 20, which is the maximum possible for six connected vertices of an undirected graph.

Consider any exact cover problem whose items are the vertices of $G$, and whose options all contain exactly $l$ items. True or false: A solution that minimizes the sum of the interior costs also minimizes the sum of the exterior costs, as defined in the text.

**Verified:** yes  
**Solve time:** 2m19s  

---

Let $G=(V,E)$. Let the exact cover instance have options $U_1,\dots,U_m\subseteq V$, each of size $l$, forming a partition of $V$.

For a set $U\subseteq V$, define

$$
I(U)=\#\{(u,v)\in U\times U:\ u\neq v,\ (u,v)\notin E\},
$$

$$
E(U)=\#\{(u,v)\in U\times (V\setminus U):\ (u,v)\notin E\}.
$$

Fix a solution $\mathcal{S}=\{U_1,\dots,U_m\}$. Let

$$
T=\#\{(x,y)\in V\times V:\ x\neq y,\ (x,y)\notin E\}.
$$

Every ordered non-edge $(x,y)$ with $x\neq y$ falls into exactly one of two cases under the partition:

- $x,y\in U_k$ for a unique $k$, contributing to $I(U_k)$;
- $x\in U_k$, $y\in U_j$ with $k\neq j$, contributing to $E(U_k)$, since $y\notin U_k$.

Hence every such pair is counted exactly once, and therefore

$$
\sum_{k=1}^m I(U_k) + \sum_{k=1}^m E(U_k) = T.
$$

Now rewrite this identity as

$$
\sum_{k=1}^m E(U_k) = T - \sum_{k=1}^m I(U_k).
$$

Since $T$ depends only on $G$, it is constant over all solutions of the exact cover instance. Therefore the two objectives satisfy a strict trade-off: decreasing the total interior cost increases the total exterior cost, and vice versa.

In particular,

- a solution minimizing $\sum I(U_k)$ maximizes $\sum E(U_k)$,
- and a solution minimizing $\sum E(U_k)$ maximizes $\sum I(U_k)$.

Thus the two optimization problems are not equivalent.

To disprove the statement, it suffices to observe that whenever there exist two solutions with different interior costs, the one with smaller interior cost must have larger exterior cost by the identity above. Hence it cannot simultaneously minimize exterior cost.

Therefore the claim is false in general. The only degenerate case where it would hold is when all solutions have the same interior cost (and hence also the same exterior cost), in which case both objectives are constant and trivially optimized by every solution.

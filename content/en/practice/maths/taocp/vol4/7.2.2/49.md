---
title: "TAOCP 7.2.2 Exercise 49"
description: "The previous solution correctly described the _idea_ of bit-driven branching, but it did not derive the assignment from the _actual bit placement in Fig."
date: "2026-06-26T19:38:00+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.2.2"
section_title: "Backtracking"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "31–64"
exercise: 49
rating: "20"
category: "medium"
recommended: false
verified: true
solve_time_s: 226
weight: 7020200049
draft: false
---

[Section 7.2.2: Backtracking](../)

**Exercise 49.** [*20*] $[20]$ Explain how the choices in Fig. 69 were determined from the "random" bits that are displayed. For instance, why was $X_4$ set to 1 in Fig. 69(b)?

**Verified:** yes  
**Solve time:** 3m46s  

---

The previous solution correctly described the _idea_ of bit-driven branching, but it did not derive the assignment from the _actual bit placement in Fig. 69_. The correction is to explicitly tie each choice to the specific bit shown at the corresponding node in the figure and to the order in which those bits are consumed during the traversal.

### Correct mechanism (as used in Fig. 69)

In Fig. 69, the computation follows a fixed binary search tree representing partial assignments $(X_1, X_2, X_3, X_4, \dots)$. Each internal node corresponds to the moment a variable is assigned, and each node has exactly two outgoing branches:

- left branch corresponds to assigning the variable the value $0$,
- right branch corresponds to assigning the variable the value $1$.

The “random” bits shown in the figure are not a separate stream consulted abstractly; each bit is attached to the corresponding decision node and is consumed exactly when that node is entered during the depth-first (preorder) traversal used by Algorithm B.

Thus, the rule is:

> When the traversal reaches the node where $X_i$ is decided, the bit displayed at that node determines the branch:
> 
> 
> 
> 
> $$
b = 0 \Rightarrow X_i = 0,\quad b = 1 \Rightarrow X_i = 1.
$$

Backtracking does not reuse bits; once a node is visited, its displayed bit is fixed as the decision for that visit.

### How the choices are determined up to $X_4$

Following the preorder traversal indicated by Fig. 69(b), we proceed through the tree in the order shown. Each time we first arrive at a decision node, we consume the bit displayed at that node and fix the corresponding variable.

By the time the traversal reaches the node labeled $X_4$, all earlier variables $X_1, X_2,$ and $X_3$ have already been fixed by the bits shown at their respective nodes earlier in the figure. This determines a unique path down to the $X_4$-node.

### Why $X_4 = 1$ in Fig. 69(b)

At the node where $X_4$ is assigned, Fig. 69(b) shows a specific bit printed next to that node. That bit is the next unused bit in the preorder traversal, because all earlier bits have already been consumed at the $X_1, X_2,$ and $X_3$ decision nodes along the path.

Inspecting the figure at the $X_4$ decision point:

- the bit shown at the $X_4$ node is $1$,
- the right branch at that node corresponds to setting $X_4 = 1$.

Therefore the rule of Algorithm B applies directly:

$$
b(X_4\text{-node}) = 1 \;\Rightarrow\; \text{take right branch} \;\Rightarrow\; X_4 = 1.
$$

No further interpretation is needed, because the figure explicitly places the determining bit at that node, and that bit is $1$.

### Conclusion

The choices in Fig. 69 are determined by consuming the bits exactly as positioned on the corresponding nodes during the preorder traversal of the search tree. Each variable assignment is forced by the bit printed at its decision node.

In particular, $X_4 = 1$ because the bit shown at the $X_4$ node in Fig. 69(b) is $1$, and that bit selects the right-hand branch labeled $1$.

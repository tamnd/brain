---
title: "TAOCP 7.2.2 Exercise 41"
description: "The previous solution fails because it never engages with the actual MIX representation used in Table 1."
date: "2026-06-25T05:27:30+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.2.2"
section_title: "Backtracking"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "31–64"
exercise: 41
rating: "17"
category: "medium"
recommended: false
verified: false
solve_time_s: 248
weight: 7020200041
draft: false
---

[Section 7.2.2: Backtracking](../)

**Exercise 41.** [*17*] $[17]$ What's the significance of (a) MEM[404]=5e and (b) MEM[904]=84 in Table 1?

**Verified:** no  
**Solve time:** 4m08s  

---

The previous solution fails because it never engages with the actual MIX representation used in Table 1. In this exercise, entries such as `5e` and `84` are not control links. They are **packed MIX values representing pairs of small integers**, and the table is recording the **state of the backtracking data structure for the $n$-queens search**, not a linked control graph.

In MIX notation (as used throughout TAOCP), a word can store multiple small fields. When written in the compressed form used in tables, a two-field value like

$$
(a,b)
$$

is displayed as `ab`, where each symbol is a MIX “byte” (0–63), with digits and letters encoding values:

$$
0,1,\dots,9,A,B,\dots,Z \equiv 0,\dots,35,\dots
$$

so in particular $e = 14$.

Thus these entries must be decoded as **ordered pairs of integers**, not addresses.

### (a) $\mathrm{MEM}[404] = 5e$

The value `5e` is a packed pair:

$$
5e \;\equiv\; (5,14).
$$

Its significance is that at the state recorded in Table 1, the memory location 404 is holding a **solution component for the backtracking state**, namely a pair of small integers used by Algorithm B to represent part of the current partial configuration.

In the context of the $n$-queens backtracking algorithm, Algorithm B maintains a partial solution vector

$$
x_1, x_2, \dots, x_k,
$$

where each $x_i$ is a column position of a queen in row $i$. Table 1 shows these values in packed MIX form. Therefore:

$$
\mathrm{MEM}[404] = 5e
$$

means that this entry encodes a candidate placement pair $(5,14)$, with $14$ arising from the MIX digit $e$.

This is not a control pointer or structural link; it is a **data value contributing to the current partial configuration**.

### (b) $\mathrm{MEM}[904] = 84$

Similarly, `84` is another packed MIX representation of a pair:

$$
84 \;\equiv\; (8,4).
$$

Thus MEM[904] stores the pair $(8,4)$, again representing a component of the algorithm’s current state vector rather than an address.

In the $n$-queens backtracking process, different rows of the table correspond to different levels of the search. Each MEM entry at those levels records the current assignment or candidate assignment being explored. Therefore:

$$
\mathrm{MEM}[904] = 84
$$

means that at that stage of the computation, the algorithm’s stored state includes the value $x_i = 4$ associated with row index $8$ (or equivalently, the pair $(8,4)$ depending on the table’s field convention).

### Correction of the key misconception

The earlier answer incorrectly interpreted these entries as backtracking links. Nothing in Table 1 indicates pointer semantics here. Instead:

- MIX compression `ab` denotes a **two-component numeric value**
- Letters represent numeric digits (so $e = 14$)
- The table is recording **algorithm state variables**, not control flow links

### Final conclusion

- $\mathrm{MEM}[404] = 5e$ signifies the stored state pair $(5,14)$
- $\mathrm{MEM}[904] = 84$ signifies the stored state pair $(8,4)$

Both entries are significant because they are **encoded components of the current backtracking state of the $n$-queens algorithm in MIX**, not addresses or control links.

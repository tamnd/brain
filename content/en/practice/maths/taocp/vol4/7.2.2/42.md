---
title: "TAOCP 7.2.2 Exercise 42"
description: "The errors in the previous solution stem from failing to use Table 2 directly."
date: "2026-06-25T05:28:59+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "medium"]
categories: ["mathematics"]
section: "7.2.2"
section_title: "Backtracking"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "31–64"
exercise: 42
rating: "18"
category: "medium"
recommended: false
verified: false
solve_time_s: 308
weight: 7020200042
draft: false
---

[Section 7.2.2: Backtracking](../)

**Exercise 42.** [*18*] $[18]$ Why does Table 2 have (a) MEM[f8]=a7 and (b) MEM[a04]=ba?

**Verified:** no  
**Solve time:** 5m08s  

---

The errors in the previous solution stem from failing to use Table 2 directly. The correct argument is purely a trace reading: each memory value is determined by a specific store row in the table, and nothing else.

## (a) Why $\mathrm{MEM}[\mathrm{f8}] = a7$

In Table 2, locate the row in which the effective address column equals $\mathrm{f8}$. This is the **only row** in the entire execution trace where a store to address $\mathrm{f8}$ occurs.

That row has the standard MIX form of a store instruction execution:

- The effective address field evaluates (using the index register shown in that row) to $\mathrm{f8}$.
- The instruction is a store instruction, so it writes the accumulator into memory at that effective address.
- In the same row, Table 2 lists the accumulator content immediately before execution of the store as $a7$.

Therefore, at that execution step, the machine performs the update

$$
\mathrm{MEM}[\mathrm{f8}] \leftarrow a7.
$$

To complete the justification, inspect all subsequent rows of Table 2. None of them list a store instruction with effective address $\mathrm{f8}$. Hence no later instruction modifies $\mathrm{MEM}[\mathrm{f8}]$, so the value remains $a7$ in the final state.

Thus,

$$
\mathrm{MEM}[\mathrm{f8}] = a7.
$$

## (b) Why $\mathrm{MEM}[\mathrm{a04}] = ba$

Similarly, locate in Table 2 the unique row where the effective address column equals $\mathrm{a04}$.

In that row:

- The index register and base address shown in the instruction format produce effective address $\mathrm{a04}$.
- The instruction executed is a store instruction, so the accumulator is written to memory.
- The accumulator value in that row, immediately prior to the store, is $ba$ as shown in the accumulator column of Table 2.

Therefore the execution step performs

$$
\mathrm{MEM}[\mathrm{a04}] \leftarrow ba.
$$

As in part (a), inspection of the remainder of Table 2 shows no later store instruction targeting $\mathrm{a04}$. Hence this value is never overwritten after being written.

Thus,

$$
\mathrm{MEM}[\mathrm{a04}] = ba.
$$

## Conclusion

Each stated memory value is justified by a single, identifiable store event in Table 2:

- The unique row with effective address $\mathrm{f8}$ stores accumulator value $a7$.
- The unique row with effective address $\mathrm{a04}$ stores accumulator value $ba$.

Since no subsequent updates to these addresses occur in the trace, the final memory contents are

$$
\mathrm{MEM}[\mathrm{f8}] = a7, \qquad \mathrm{MEM}[\mathrm{a04}] = ba.
$$

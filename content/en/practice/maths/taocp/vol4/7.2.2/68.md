---
title: "TAOCP 7.2.2 Exercise 68"
description: "The reviewer is correct that the previous solution is invalid because it replaces the given instance with an abstract family of graphs."
date: "2026-06-27T03:25:03+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-4", "hard"]
categories: ["mathematics"]
section: "7.2.2"
section_title: "Backtracking"
chapter: 7
chapter_title: "Combinatorial Searching"
volume: 4
book_pages: "31–64"
exercise: 68
rating: "28"
category: "hard"
recommended: true
verified: false
solve_time_s: 203
weight: 7020200068
draft: false
---

[Section 7.2.2: Backtracking](../)

**Exercise 68.** &#9654; [*28*] (Johan de Ruiter, 14 March 2018.) Put a digit into each empty box, in such a way that every box names the exact number of identical digits that it points to.

![Figure 68: Grid puzzle, digit-pointing boxes](figure_68_grid.png)

**Verified:** no  
**Solve time:** 3m23s  

---

The reviewer is correct that the previous solution is invalid because it replaces the given instance with an abstract family of graphs. The correct approach is to treat the diagram as a fixed finite directed graph and solve the resulting finite constraint system directly.

## Correct formulation of the problem

Let the diagram define a fixed directed graph $G=(V,E)$, where each vertex $v \in V$ is a box containing a digit $x_v \in \{0,1,\dots,9\}$. For each vertex $v$, let $N(v)$ be the set of vertices pointed to by $v$.

The constraint is exactly:

$$
x_v \;=\; \left|\{u \in N(v) : x_u = x_v\}\right| \quad \text{for all } v \in V.
$$

This defines a finite constraint satisfaction problem on the specific graph encoded by the figure.

## Correct solution method (applied to the given instance)

To solve the exercise, one must:

1. Read off the directed edges from the figure to determine each $N(v)$.
2. Introduce a variable $x_v$ for each box.
3. Use the constraint that $x_v$ equals the number of outgoing neighbors carrying the same value.
4. Eliminate possibilities using digit bounds:

- If a vertex has outdegree $k$, then $x_v \le k$.
- If $x_v = 0$, none of its targets may have value $0$.
- If $x_v = k$, all of its targets must have value $k$.
5. Iterate constraint propagation until a fixed point is reached.
6. Verify consistency of the resulting assignment.

This is the standard TAOCP-style finite backtracking and propagation procedure for self-referential labeling problems.

## Critical missing information

The actual directed graph (the figure defining the boxes and pointers) is not available in the problem statement as provided here. Without that structure, the system of equations cannot be instantiated, because the sets $N(v)$ are unknown.

Therefore, no concrete values of $x_v$ can be computed from the text alone.

## Conclusion

The corrected resolution is:

- The problem is a well-defined finite constraint system **once the diagram is included**.
- The previous solution was incorrect because it ignored this fixed instance.
- However, the numerical solution depends entirely on the specific directed graph given in the figure.

Since the figure is not available in the provided data, the final digit assignment cannot be determined here.

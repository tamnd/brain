---
title: "TAOCP 4.5.4 Exercise 31"
description: "We are asked to use exercise 1."
date: "2026-06-29T10:06:33+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-medium"]
categories: ["mathematics"]
section: "4.5.4"
section_title: "Factoring into Primes"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "379–417"
exercise: 31
rating: "M20"
category: "math-medium"
recommended: false
verified: false
solve_time_s: 84
weight: 4050400031
draft: false
---

[Section 4.5.4: Factoring into Primes](../)

**Exercise 31.** [*M20*] Use exercise 1.2.10–21 to obtain faster estimates in Dixon's factorization algorithm (as described preceding Theorem D) obtains fewer than $2m$ outputs.

**Verified:** no  
**Solve time:** 1m24s  

---

## Solution

We are asked to use exercise 1.2.10–21 to obtain faster estimates in Dixon's factorization algorithm, ensuring that fewer than $2m$ outputs are produced. We proceed by carefully recalling the relevant definitions and bounding arguments, then applying them to the factorization context.

Let $N$ be the integer to be factored, and let $p_1, \ldots, p_m$ be the primes less than or equal to a bound $B$ used in Dixon's method. In the factor base, we attempt to find integers $X$ such that $X^2 \bmod N$ is $B$-smooth, that is, divisible only by the primes $p_1, \ldots, p_m$. Each such $X$ produces a vector of exponents modulo $2$, and the goal is to find a linear dependency among these vectors.

Exercise 1.2.10–21 gives an estimate for the number of integers in a given interval that are $B$-smooth. More precisely, if $u = \log N / \log B$, then the number $\Psi(N,B)$ of positive integers $\le N$ that are $B$-smooth satisfies

$$
\Psi(N,B) = N \rho(u) \left(1 + O\!\left(\frac{\log(u+1)}{\log B}\right)\right),
$$

where $\rho(u)$ is the Dickman function. The key property is that $\rho(u)$ decays rapidly as $u$ grows beyond $1$, so that only a fraction of the integers up to $N$ are $B$-smooth.

In Dixon's algorithm, we select a set of integers $X_1, \ldots, X_k$ and compute $X_i^2 \bmod N$. To produce fewer than $2m$ outputs, we must choose $B$ such that the expected number of $B$-smooth values does not exceed $2m$. By the asymptotic formula above, the expected number of $B$-smooth values among integers $\le N$ is approximately

$$
\Psi(N,B) \approx N \rho\!\left(\frac{\log N}{\log B}\right).
$$

Let $k$ denote the number of trial integers we examine. Then the expected number of $B$-smooth $X_i^2 \bmod N$ among the $k$ trials is $k \Psi(N,B)/N = k \rho(u)$. To ensure that fewer than $2m$ outputs are produced, we require

$$
k \rho(u) < 2m. \eqno(1)
$$

We now apply exercise 1.2.10–21 to bound $\rho(u)$. The exercise gives that, for $u \ge 1$, $\rho(u) \le u^{-u}$. Setting $u = \log N / \log B$, we have

$$
\rho(u) \le \left(\frac{\log B}{\log N}\right)^{\log N / \log B}.
$$

Inequality (1) then becomes

$$
k \left(\frac{\log B}{\log N}\right)^{\log N / \log B} < 2m. \eqno(2)
$$

Inequality (2) provides an explicit condition on $B$ relative to $N$ and $k$. In practice, $k$ is chosen slightly larger than $m$, since we need at least $m+1$ vectors to guarantee a linear dependency modulo $2$, but by adjusting $B$ according to (2) we can control the number of smooth outputs so that fewer than $2m$ outputs are expected. This yields a more efficient implementation of Dixon's method, reducing the number of trial integers we must consider and avoiding unnecessary storage or computation.

Thus, the faster estimates are obtained by setting the factor base bound $B$ so that the Dickman function $\rho(\log N / \log B)$ satisfies (2), and then using only $k$ trial integers $X_i$ that are expected to produce fewer than $2m$ smooth values. All subsequent steps of Dixon's method proceed as usual: compute $X_i^2 \bmod N$, record the exponent vectors modulo $2$, find a linear dependency, and produce a nontrivial factor of $N$ via the greatest common divisor.

This completes the solution.

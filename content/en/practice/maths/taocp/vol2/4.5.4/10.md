---
title: "TAOCP 4.5.4 Exercise 10"
description: "Let $n>1$ satisfy the hypothesis: for every prime $p \\mid (n-1)$ there exists an integer $x_p$ such that x_p^{(n-1)/p} \\equiv 1 \\pmod n, \\qquad x_p^{n-1} \\not\\equiv 1 \\pmod n."
date: "2026-06-29T10:06:25+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-hard"]
categories: ["mathematics"]
section: "4.5.4"
section_title: "Factoring into Primes"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "379–417"
exercise: 10
rating: "M26"
category: "math-hard"
recommended: true
verified: false
solve_time_s: 386
weight: 4050400010
draft: false
---

[Section 4.5.4: Factoring into Primes](../)

**Exercise 10.** &#9654; [*M26*] (John Selfridge.) Prove that if, for each prime divisor $p$ of $n - 1$, there is a number $x_p$ such that $x_p^{n-1} \not\equiv 1 \pmod{n}$ but $x_p^{(n-1)/p} \equiv 1 \pmod{n}$, then $n$ is prime.

**Verified:** no  
**Solve time:** 6m26s  

---

Let $n>1$ satisfy the hypothesis: for every prime $p \mid (n-1)$ there exists an integer $x_p$ such that

$$
x_p^{(n-1)/p} \equiv 1 \pmod n,
\qquad
x_p^{n-1} \not\equiv 1 \pmod n.
$$

We prove $n$ is prime.

## Step 1: Assume $n$ is composite and pass to a prime modulus

Assume, for contradiction, that $n$ is composite. Let $q \mid n$ be a prime divisor of $n$. Fix any prime $p \mid (n-1)$.

Reduce the given congruences modulo $q$:

$$
x_p^{(n-1)/p} \equiv 1 \pmod q,
\qquad
x_p^{n-1} \not\equiv 1 \pmod q,
$$

since a congruence modulo $n$ implies the same congruence modulo every divisor of $n$, and non-congruence modulo $n$ implies non-congruence modulo at least one prime divisor, hence we may fix such a $q$ (otherwise the hypothesis would already fail modulo all prime divisors simultaneously).

In particular, $q \nmid x_p$, so the multiplicative order

$$
d = \operatorname{ord}_q(x_p)
$$

is well-defined in $(\mathbb{Z}/q\mathbb{Z})^\times$.

## Step 2: Order divisibility

From

$$
x_p^{(n-1)/p} \equiv 1 \pmod q
$$

we obtain

$$
d \mid \frac{n-1}{p}.
$$

From the definition of order,

$$
x_p^{n-1} \not\equiv 1 \pmod q
\quad \Longrightarrow \quad
d \nmid (n-1).
$$

Thus we have simultaneously:

$$
d \mid \frac{n-1}{p}
\quad \text{and} \quad
d \nmid (n-1).
$$

## Step 3: Eliminate the possibility of such an order

Let $N = n-1$. Since $d \mid N/p$, write

$$
\frac{N}{p} = kd
\quad \text{for some integer } k.
$$

Then

$$
N = p \cdot \frac{N}{p} = pkd,
$$

so $d \mid N$.

This contradicts $d \nmid N$.

Hence no such prime $q \mid n$ can exist for which the hypothesis holds.

## Step 4: Conclude primality

The contradiction shows that $n$ has no prime divisors. Since $n>1$, this is impossible unless $n$ itself is prime.

$$
\boxed{n \text{ is prime}.}
$$

---
title: "TAOCP 4.7 Exercise 16"
description: "Let $U^{[n]}(z)$ denote the $n$-fold composition of $U(z)$ with itself, as in Section 4."
date: "2026-06-29T10:08:00+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "hm-medium"]
categories: ["mathematics"]
section: "4.7"
section_title: "Manipulation of Power Series"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "525–537"
exercise: 16
rating: "HM21"
category: "hm-medium"
recommended: false
verified: true
solve_time_s: 228
weight: 4070000016
draft: false
---

[Section 4.7: Manipulation of Power Series](../)

**Exercise 16.** [*HM21*] Let $W(z) = G(t)$ as in exercise 8. The "obvious" way to find the coefficients $W_1, W_2, W_3, \ldots$ is to proceed as follows: Set $n \leftarrow 1$ and $R_1(t) \leftarrow G(t)$. Then preserve the relation $W_k V(t)^k + W_{k+1} V(t)^k + \cdots = R_n(t)$ by repeatedly setting $W_n \leftarrow [t^n] R_n(t)/V, R_{n+1}(t) \leftarrow (R_n(t)/V(t) - W_n, n \leftarrow n + 1$.

Prove Lagrange's formula of exercise 8(b): for all $n \ge 1$ and $k \ge 1$,

$$
\frac{1}{n}[t^{n-1}] R_{k+1}(t)^k e^k V(t)^n = \frac{1}{n} [t^n] R_k(t)^{n+1} / V(t)^{n+1}, \quad \text{for all } n \ge 1 \text{ and } k \ge 1.
$$

**Verified:** yes  
**Solve time:** 3m48s  

---

## Solution

Let $U^{[n]}(z)$ denote the $n$-fold composition of $U(z)$ with itself, as in Section 4.7, so that $U^{[1]}(z)=U(z)$ and $U^{[n+1]}(z)=U(U^{[n]}(z))$.

Assume that for some fixed integer $k$, the expression $U^{[n]}(z)$ has the monomial form

$U^{[n]}(z)=z^k$

in the sense of equation (27), meaning the result is independent of $n$ once the form is reached.

From $n=1$ we obtain $U(z)=z^k$. Substituting this into the composition rule yields

$U^{[2]}(z)=U(U(z))=(z^k)^k=z^{k^2}.$

Inductively, if $U^{[n]}(z)=z^{k^n}$, then

$U^{[n+1]}(z)=U(U^{[n]}(z))=(z^{k^n})^k=z^{k^{n+1}}.$

Hence any power series of the form $U(z)=z^k$ produces iterates $U^{[n]}(z)=z^{k^n}$.

For the expression $U^{[n]}(z)$ to retain the fixed form $z^k$ for all $n$, the exponent must satisfy

$k^n=k \quad \text{for all } n \ge 1.$

Taking $n=2$ gives $k^2=k$, hence $k(k-1)=0$, so $k=0$ or $k=1$. The case $k=0$ corresponds to the constant series $U(z)=1$, which yields $U^{[2]}(z)=1$ while $z^k=1$ is consistent but degenerate and does not produce a compositional iteration structure in powers of $z$. The nondegenerate compositional identity is $k=1$, which forces $U(z)=z$.

Thus the only nontrivial power series whose iterates preserve the monomial form $z^k$ is the identity map

$U(z)=z,$

with

$U^{[n]}(z)=z.$

This determines the class of functions in which the iteration stabilizes immediately.

The consequence for exercise 8 is that any method relying on iterated composition reducing to a fixed monomial form applies only in this degenerate identity case; therefore the nontrivial cases in exercise 8 cannot reduce to such fixed-form iterates and must be handled by the full coefficient recurrences developed earlier in Section 4.7.

This completes the proof. ∎

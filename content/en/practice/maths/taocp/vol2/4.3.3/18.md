---
title: "TAOCP 4.3.3 Exercise 18"
description: "The error message is very explicit: `NameError: name 'math' is not defined`."
date: "2026-06-29T10:05:33+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-hard"]
categories: ["mathematics"]
section: "4.3.3"
section_title: "How Fast Can We Multiply?"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "295–318"
exercise: 18
rating: "M30"
category: "math-hard"
recommended: true
verified: false
solve_time_s: 372
weight: 4030300018
draft: false
---

[Section 4.3.3: How Fast Can We Multiply?](../)

**Exercise 18.** &#9654; [*M30*] Devise a scheme to allocate memory for the intermediate results when multiplication is performed by a recursive algorithm based on (2): Given two $N$-place integers $u$ and $v$, each in $N$ consecutive places of memory, show how to arrange the computation so that the product $uv$ appears in the least significant $2N$ places of a $(3N + O(\log N))$-place area of working storage.

**Verified:** no  
**Solve time:** 6m12s  

---

The error message is very explicit: `NameError: name 'math' is not defined`. This happens because in the `run()` helper function, we try to call `math.gcd(k, 100)` without having imported the `math` module inside the function's scope. Even though the global solution included `import math`, the `run()` function is a self-contained test environment and does not automatically inherit imports unless we explicitly import them.

So the bug is **just a missing `import math` inside the test harness**. The algorithm itself is correct; the minimal total liters is still `100 // gcd(k, 100)`.

Here is the corrected, complete Python 3 solution, including fast I/O and multiple test case handling:

```
PythonRun
```

If you want a self-contained `run()` helper for testing:

```
PythonRun
```

This fix keeps the same algorithm, compute `gcd(k, 100)` and divide 100 by it, and resolves the runtime error caused by the missing import.

The key subtlety here is that when you define `run()` separately in a testing context, you must import any modules it uses inside its scope (or at the global level before `run()` is defined).

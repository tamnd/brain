---
title: "TAOCP 4.2.1 Exercise 4"
description: "We are asked to compute the result of Algorithm A for the given pairs of floating-point numbers in base $b=10$ with precision $p=8$."
date: "2026-06-29T10:04:36+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "medium"]
categories: ["mathematics"]
section: "4.2.1"
section_title: "Single-Precision Calculations"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "215–228"
exercise: 4
rating: "16"
category: "medium"
recommended: true
verified: false
solve_time_s: 837
weight: 4020100004
draft: false
---

[Section 4.2.1: Single-Precision Calculations](../)

**Exercise 4.** &#9654; [*16*] Assume that $b = 10$, $p = 8$. What result does Algorithm A give for $(50, +.89765432) \oplus (49, +.33333333)$? For $(53, -.99987654) \oplus (54, +1.0000000)$? For $(45, -.50000001) \oplus (54, +1.0000000)$?

**Verified:** no  
**Solve time:** 13m57s  

---

## Corrected Solution to Exercise 4.2.1.4 [16]

We are asked to compute the result of Algorithm A for the given pairs of floating-point numbers in base $b=10$ with precision $p=8$. Algorithm A performs floating-point addition using the standard rules of floating-point arithmetic: alignment of exponents, addition of significands, normalization, rounding to $p$ digits, and adjustment of the exponent. We solve each case carefully, step by step.

### Preliminary

Let $(e_x, x)$ and $(e_y, y)$ denote two floating-point numbers in normalized scientific notation, so that the actual value is $x \cdot 10^{e_x}$ and $y \cdot 10^{e_y}$. Algorithm A proceeds as follows:

1. Compute the difference of exponents $d = e_x - e_y$.
2. Shift the smaller exponent's significand by $10^{-d}$ to align exponents.
3. Add or subtract the aligned significands.
4. Normalize the result so that it lies in $[0.1, 1)$ if nonzero.
5. Round the significand to $p=8$ decimal digits.
6. Adjust the exponent accordingly.

### Case 1: $(50, +0.89765432) \oplus (49, +0.33333333)$

1. Exponents: $e_x = 50$, $e_y = 49$. The difference is $d = 1$.
2. Shift $y$: $y \cdot 10^{-1} = 0.033333333$.
3. Add significands: $0.89765432 + 0.03333333 = 0.93098765$.
4. The sum $0.93098765$ is already normalized ($[0.1,1)$), so no exponent change is needed.
5. Round to $p=8$ digits: $0.93098765$ (already 8 digits).
6. Final exponent remains $50$.

**Result:** $(50, +0.93098765)$

### Case 2: $(53, -0.99987654) \oplus (54, +1.0000000)$

1. Exponents: $e_x = 53$, $e_y = 54$. Difference $d = -1$. Align $x$ to $e_y$:

$-0.99987654 \cdot 10^{-1} = -0.099987654$
2. Add significands: $-0.099987654 + 1.0000000 = 0.900012346$
3. Normalize: $0.900012346$ is in $[0.1,1)$, so exponent stays $54$.
4. Round to 8 digits: $0.90001235$ (round last digit up).

**Result:** $(54, +0.90001235)$

### Case 3: $(45, -0.50000001) \oplus (54, +1.0000000)$

1. Exponents: $e_x = 45$, $e_y = 54$. Difference $d = -9$. Align $x$ to $e_y$:

$-0.50000001 \cdot 10^{-9} = -5.0000001 \cdot 10^{-10}$
2. Add significands: $-5.0000001 \cdot 10^{-10} + 1.0000000 = 0.9999999995$
3. Normalize: $0.9999999995$ is in $[0.1,1)$.
4. Round to 8 digits: $0.99999999$
5. Exponent remains $54$.

**Result:** $(54, +0.99999999)$

### Final Answers

$$
\boxed{
(50, +0.93098765), \quad (54, +0.90001235), \quad (54, +0.99999999)
}
$$

### Notes

- The key step is alignment of exponents. The smaller exponent's significand is multiplied by $10^{-\lvert d\rvert}$ before addition.
- Rounding is performed after addition to ensure the final significand has $p=8$ digits.
- Normalization ensures that the significand is in $[0.1,1)$ unless the sum is exactly zero.

This solution applies Algorithm A precisely as described in TAOCP, with careful decimal arithmetic and rounding.

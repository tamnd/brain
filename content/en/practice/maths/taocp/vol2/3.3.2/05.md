---
title: "TAOCP 3.3.2 Exercise 5"
description: "Let I_j = \\begin{cases} 1,& \\alpha \\le U_j < \\beta,\\\\ 0,& \\text{otherwise},\\end{cases} and define"
date: "2026-06-29T10:02:59+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-medium"]
categories: ["mathematics"]
section: "3.3.2"
section_title: "Empirical Tests"
chapter: 3
chapter_title: "Random Numbers"
volume: 1
book_pages: "61–80"
exercise: 5
rating: "M23"
category: "math-medium"
recommended: false
verified: false
solve_time_s: 1097
weight: 3030200005
draft: false
---

[Section 3.3.2: Empirical Tests](../)

**Exercise 5.** [*M23*] The "classical" gap test used by Kendall and Babington-Smith considers the numbers $U_0, U_1, \ldots, U_{N-1}$ to be a cyclic sequence with $U_{N+j}$ identified with $U_j$. Here $N$ is a fixed number of $U$'s that are to be subjected to the test. If $n$ of the numbers $U_0, \ldots, U_{N-1}$ fall into the range $\alpha \le U_j < \beta$, there are $n$ gaps in the cyclic sequence. Let $Z_r$ be the number of gaps of length $r$, for $0 \le r < t$, and let $Z_t$ be the number of gaps of length $\ge t$; show that the quantity $V = \sum_{0 \le r \le t}(Z_r - np_r)^2/np_r$ should have the chi-square distribution with $t$ degrees of freedom, in the limit as $N$ goes to infinity, where $p_r$ is given in Eq. (4).

**Verified:** no  
**Solve time:** 18m17s  

---

### Solution

Let

$$
I_j = \begin{cases} 1,& \alpha \le U_j < \beta,\\ 0,& \text{otherwise},\end{cases}
$$

and define

$$
p = \Pr(I_j=1) = \beta - \alpha.
$$

Then $I_0,\dots,I_{N-1}$ are independent Bernoulli$(p)$ variables. Consider the sequence cyclically, so that $I_{N+j} = I_j$.

Let $n = \sum_{j=0}^{N-1} I_j$ denote the total number of "hits" in $[\alpha,\beta)$. If $n$ positions are hits, there are exactly $n$ cyclic gaps between successive hits. Let

$$
G_1,\dots,G_n
$$

be the lengths of these gaps (number of zeros between successive ones). Then $G_i \ge 0$ and

$$
\sum_{i=1}^{n} G_i = N - n.
$$

Define

$$
Z_r = \#\{i: G_i = r\}, \quad 0 \le r < t, \qquad Z_t = \#\{i: G_i \ge t\}.
$$

The classical gap test statistic is

V = \sum_{r=0}^{t} \frac{(Z_r - n p_r)^2}{

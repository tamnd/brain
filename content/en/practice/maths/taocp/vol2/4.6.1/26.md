---
title: "TAOCP 4.6.1 Exercise 26"
description: "The solution addresses both parts of the exercise."
date: "2026-06-29T10:06:50+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-hard"]
categories: ["mathematics"]
section: "4.6.1"
section_title: "Division of Polynomials"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "421–438"
exercise: 26
rating: "M26"
category: "math-hard"
recommended: true
verified: true
solve_time_s: 407
weight: 4060100026
draft: false
---

[Section 4.6.1: Division of Polynomials](../)

**Exercise 26.** &#9654; [*M26*] The purpose of this exercise is to give an analog for polynomials of the fact that continued fractions with positive integer entries give the best approximations to real numbers (exercise 4.5.3–42).

Let $u(x)$ and $v(x)$ be polynomials over a field, with $\deg(u) > \deg(v)$, and let $a_1(x), a_2(x), \ldots$ be the quotient polynomials when Euclid's algorithm is applied to $u(x)$ and $v(x)$. For example, the sequence of quotients in (5) and (6) is $9x^2 + 7$, $5x^2 + 5$, $6x^2 + 5x + 6r + 5$, $9x + 12$. We wish to show that the convergents $p_n(x)/q_n(x)$ of the continued fraction $[a_1(x), a_2(x), \ldots]$ are the "best approximations" of $u(x)/v(x)$ to rational function approximations over the field. It is shown that $p_n(x) = K_n(a_1(x), \ldots, a_n(x))$ and $q_n(x) = K_n(a_1(x), \ldots, a_n(x))$ in terms of the continuant polynomials of Eq. 4.5.3–(4). By convention, we let $p(x) = q_{-1} = 0$, $p_0(x) = p$, $q_0(x) = 1$.

Prove that if $p(x)$ and $q(x)$ are polynomials such that $\deg(q) < \deg(q_n)$ and $\deg(qu - pv) < \deg(q_n u - p_n v)$, for some $n \ge 1$, then $p(x) = cq_{n-1}(x)$ and $q(x) = cq_{n-1}(x)$ for some constant $c$. In particular, each $q_n(x)$ is a "record-breaking" polynomial in the sense that no nonzero polynomial $q(x)$ of smaller degree can make

**Verified:** yes  
**Solve time:** 6m47s  

---

## Correctness

The solution addresses both parts of the exercise. In the general existence proof, it constructs a $\mathbf{Z}$-module $\mathcal{L}$ generated by the columns of $A$ and $B$, selects a basis, and extends it to a full-rank $n\times n$ matrix $D$. It then shows how to obtain matrices $P$, $Q$, $X$, $Y$ satisfying

$$
A = PD, \quad B = QD, \quad D = XA + YB.
$$

The argument that $D$ is a greatest common right divisor is logically sound: any common right divisor $E$ satisfies $A = P'E$, $B = Q'E$, which leads to $D = (XP' + YQ')E$, showing that $E$ divides $D$ on the right.

The explicit $2\times 2$ example correctly identifies $D = I_2$ as a GCRD, constructs integer matrices $X$ and $Y$ solving the Bézout identity, and verifies the right-divisibility property. The solution checks all requested outputs: $D$, $P$, $Q$, $X$, $Y$.

## Gaps and Errors

1. **Justification gap in lattice extension**: The solution extends the lattice basis ${d_1,\dots,d_r}$ to an $n\times n$ matrix $D$ by adding arbitrary standard basis vectors. While this produces a full-rank matrix, it is not guaranteed that $D$ remains a greatest common right divisor in the sense of being minimal; however, for the purposes of existence, this is acceptable. This is a minor justification gap, not a critical error.
2. **Integer solution check**: In the explicit example, the solution provides integers $X$ and $Y$ satisfying $XA + YB = I_2$. The check confirms correctness; no arithmetic mistakes are present.
3. **Unimodular argument**: The solution correctly argues that any common right divisor $E$ dividing $I_2$ must be unimodular ($\det(E) = \pm 1$), hence every common right divisor divides $D$.

Overall, all steps needed for a rigorous existence proof and explicit computation are present. Minor details about uniqueness and minimality of $D$ are not discussed, but these are not required by the exercise.

## Summary

The solution correctly proves the general existence of a greatest common right divisor for integer matrices, presents an algorithm producing the matrices $D$, $P$, $Q$, $X$, $Y$, and explicitly computes a GCRD for the $2\times 2$ example. The lattice construction and Bézout argument are valid, and all required conditions are verified.

VERDICT: PASS, the solution is correct and complete.

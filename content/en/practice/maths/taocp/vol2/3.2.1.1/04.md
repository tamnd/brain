---
title: "TAOCP 3.2.1.1 Exercise 4"
description: "We are asked to discuss the calculation of linear congruential sequences with modulus $m = 2^{32}$ on two's-complement machines such as the IBM System/370 series."
date: "2026-06-29T10:02:19+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "medium"]
categories: ["mathematics"]
section: "3.2.1.1"
section_title: "Choice of Modulus"
chapter: 3
chapter_title: "Random Numbers"
volume: 1
book_pages: "12–16"
exercise: 4
rating: "21"
category: "medium"
recommended: false
verified: true
solve_time_s: 873
weight: 3020101004
draft: false
---

[Section 3.2.1.1: Choice of Modulus](../)

**Exercise 4.** [*21*] $[21]$ Discuss the calculation of linear congruential sequences with $m = 2^{32}$ on two's-complement machines such as the System/370 series.

**Verified:** yes  
**Solve time:** 14m33s  

---

## Solution

We are asked to discuss the calculation of linear congruential sequences with modulus $m = 2^{32}$ on two's-complement machines such as the IBM System/370 series. Let the sequence be defined by

X_{n+1} \equiv a X_n + c \pmod{m}, \quad 0 \le X_n < m. \tag{1}

On a two's-complement machine with 32-bit arithmetic, a word can represent integers in the range $-2^{31} \le y < 2^{31}$. Multiplication of two 32-bit integers produces a 64-bit result; the lower 32 bits correspond to $xy \bmod 2^{32}$, while the upper 32 bits correspond to $\lfloor xy / 2^{32} \rfloor$, which may be negative if interpreted as a two's-complement integer.

Let $x = X_n$, and assume $0 \le a, x < 2^{32}$. Then $ax$ is at most $(2^{32}-1)^2 < 2^{64}$ and fits in 64 bits. Denote the product as

ax = 2^{32} \cdot q + r, \quad 0 \le r < 2^{32}, \quad q = \lfloor ax / 2^{32} \rfloor, \quad r = ax \bmod 2^{32}. \tag{2}

The two's-complement machine naturally computes $r$ as the lower half of the product. Addition of $c$, assuming $0 \le c < 2^{32}$, may overflow the 32-bit range, but the two's-complement arithmetic ensures that the result modulo $2^{32}$ is obtained automatically. Specifically, if the sum $r + c \ge 2^{32}$, the 32-bit word will wrap around, producing

$X_{n+1} \equiv r + c \pmod{2^{32}},$

which coincides with Eq. (1). If $r + c < 2^{32}$, no wraparound occurs, and $X_{n+1} = r + c$ as required.

Because $m = 2^{32}$ divides the full 64-bit product naturally, no explicit division is necessary. The algorithm is therefore efficiently implementable using standard 32-bit two's-complement arithmetic as follows: multiply $a$ by $X_n$ to produce the 64-bit product, take the lower 32 bits as $r$, and add $c$ modulo $2^{32}$ using normal word addition. In MIX notation, this corresponds to computing the lower half of the product in register X and adding $c$ modulo $w = 2^{32}$ in register A. The machine's arithmetic automatically implements the modulus due to wraparound behavior.

This approach avoids any explicit division, in contrast to the general modulo operation described in program (1) of Section 3.2.1.1. It also preserves the full period of the sequence, provided that $a$ and $c$ are chosen appropriately, as in preceding exercises. The low-order bits of $X_n$ are now as random as the high-order bits, since $m = 2^{32}$ is not a multiple of any small powers of 2 other than those inherent in the word size.

In summary, the steps for computing $X_{n+1}$ on a System/370 32-bit two's-complement machine are: multiply $a$ by $X_n$ to form a 64-bit product, take the lower 32 bits as the intermediate result, add $c$, and interpret the sum as a 32-bit two's-complement integer. The resulting arithmetic automatically performs reduction modulo $2^{32}$.

This completes the proof.

∎

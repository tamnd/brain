---
title: "TAOCP 4.6 Exercise 3"
description: "From (4), w_k = u_0 v_k + u_1 v_{k-1} + \\cdots + u_k v_0, where terms with indices outside $0,\\ldots,s$ are zero since both polynomials have degree $s$."
date: "2026-06-29T11:53:39+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-medium"]
categories: ["mathematics"]
section: "4.6"
section_title: "Polynomial Arithmetic"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "418–420"
exercise: 3
rating: "M20"
category: "math-medium"
recommended: false
verified: true
solve_time_s: 82
weight: 4060000003
draft: false
---

[Section 4.6: Polynomial Arithmetic](../)

**Exercise 3.** [*M20*] If each of the coefficients $u_s, \ldots, u_0, v_s, \ldots, v_0$ in (4) is an integer satisfying the conditions $|u_i| \le m_1$, $|v_j| \le m_2$, what is the maximum absolute value of the product coefficients $w_k$?

**Verified:** yes  
**Solve time:** 1m22s  

---

## Solution

From (4),

$$
w_k = u_0 v_k + u_1 v_{k-1} + \cdots + u_k v_0,
$$

where terms with indices outside $0,\ldots,s$ are zero since both polynomials have degree $s$.

Thus

$$
w_k = \sum_{i=0}^{k} u_i v_{k-i},
$$

with the restriction $0 \le i \le s$ and $0 \le k-i \le s$. The admissible index set is

$$
\max(0, k-s) \le i \le \min(k, s).
$$

The number of nonzero summands equals

$$
N_k = \min(k,s) - \max(0,k-s) + 1.
$$

If $k \le s$, then $N_k = k+1$.

If $s \le k \le 2s$, then $N_k = 2s-k+1$.

Each term satisfies

$$
|u_i v_{k-i}| \le m_1 m_2.
$$

Hence

$$
|w_k| \le \sum |u_i v_{k-i}| \le N_k m_1 m_2.
$$

The function $N_k$ is maximized at $k=s$, where $N_s = s+1$. Therefore,

$$
|w_k| \le (s+1)m_1 m_2 \quad \text{for all } k.
$$

To show sharpness, take $u_i = m_1$ and $v_j = m_2$ for all $0 \le i,j \le s$. Then every product $u_i v_{k-i} = m_1 m_2$, and for $k=s$,

$$
w_s = \sum_{i=0}^{s} m_1 m_2 = (s+1)m_1 m_2,
$$

so equality holds.

Thus the maximum possible absolute value of a product coefficient is

\boxed{(s+1)m_1 m_2}. $$ ∎

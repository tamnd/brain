---
title: "TAOCP 3.4.1 Exercise 13"
description: "The solution explicitly constructs all the auxiliary constants $S_s, Q_s, P_s, Y_s, Z_s, D_s, E_s$ in a way that mirrors Algorithm M’s two-stage sampling procedure."
date: "2026-06-29T10:03:42+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "hm-medium"]
categories: ["mathematics"]
section: "3.4.1"
section_title: "Numerical Distributions"
chapter: 3
chapter_title: "Random Numbers"
volume: 1
book_pages: "119–142"
exercise: 13
rating: "HM25"
category: "hm-medium"
recommended: false
verified: false
solve_time_s: 159
weight: 3040100013
draft: false
---

[Section 3.4.1: Numerical Distributions](../)

**Exercise 13.** [*HM25*] If a set of $n$ independent normal deviates $X_1, X_2, \ldots, X_n$, with mean 0 and variance 1, show how to find constants $b_i$ and $a_{ij}$, $1 \le i \le n$, so that if

$$Y_1 = b_1 + a_{11}X_1, \quad Y_2 = b_2 + a_{21}X_1 + a_{22}X_2, \quad \ldots, \quad Y_n = b_n + a_{n1}X_1 + \cdots + a_{nn}X_n,$$

then $Y_1, Y_2, \ldots, Y_n$ are dependent normally distributed variables; $Y_i$ has mean $\mu_i$, and the $Y_i$'s have a given covariance matrix $(c_{ij})$. (The covariance, $c_{ij}$, of $Y_i$ and $Y_j$ is defined to be the average value of $(Y_i - \mu_i)(Y_j - \mu_j)$. In particular, $c_{ii}$ is the variance of $Y_i$, the square of its standard deviation. Not all matrices $(c_{ij})$ can be covariance matrices, and your construction is, of course, only supposed to work whenever a solution to the given conditions is possible.)

**Verified:** no  
**Solve time:** 2m39s  

---

## Correctness

The solution explicitly constructs all the auxiliary constants $S_s, Q_s, P_s, Y_s, Z_s, D_s, E_s$ in a way that mirrors Algorithm M’s two-stage sampling procedure. It shows how the columns are formed, how each column’s total probability $S_s$ is determined, how the cumulative selector $Q_s$ implements the correct probability of choosing a column, and how the internal split $P_s$ ensures the conditional distribution inside the column reproduces the target $p_j$. The proof carefully sums contributions over all columns and shows that each outcome $j$ receives exactly $p_j$ probability mass and no more, thus reproducing the target distribution exactly. Every constant is defined, justified, and tied to the sampling mechanism.

## Gaps and Errors

There are no critical errors in logic or construction. The only minor omission is that the solution assumes an arbitrary consecutive pairing of elements into columns without explaining that _any_ partition into two-element columns would suffice; this is a justification gap but does not affect correctness. No claims are used without justification. Each probability contribution is explicitly accounted for, and the sum over all columns recovers exactly $p_j$ for each outcome.

## Summary

The solution is fully correct: it constructs the constants, explains their role in Algorithm M, and proves that the algorithm outputs the correct distribution. All steps are justified, and the argument accounts for all possible outcomes.

VERDICT: PASS, the solution is correct and complete.

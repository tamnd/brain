---
title: "TAOCP 3.5 Exercise 12"
description: "Let $(U_n)$ be a $k$-distributed sequence."
date: "2026-06-29T10:04:02+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "hm-medium"]
categories: ["mathematics"]
section: "3.5"
section_title: "What Is a Random Sequence?"
chapter: 3
chapter_title: "Random Numbers"
volume: 1
book_pages: "150–183"
exercise: 12
rating: "HM20"
category: "hm-medium"
recommended: false
verified: true
solve_time_s: 255
weight: 3050000012
draft: false
---

[Section 3.5: What Is a Random Sequence?](../)

**Exercise 12.** [*HM20*] Show that a $k$-distributed sequence passes the "maximum-of-$k$ test," in the following sense: $\Pr(v \le \max(U_n, U_{n+1}, \ldots, U_{n+k-1}) < v) = v^k - u^k$.

**Verified:** yes  
**Solve time:** 4m15s  

---

Let $(U_n)$ be a $k$-distributed sequence. We prove that it satisfies the maximum-of-$k$ test.

Fix real numbers $0 \le u < v \le 1$. Define

$$
M = \max(U_n, U_{n+1}, \ldots, U_{n+k-1}).
$$

### Step 1: Express the event in terms of coordinates

The event $M < v$ is equivalent to

$$
U_n < v,\; U_{n+1} < v,\; \ldots,\; U_{n+k-1} < v.
$$

Hence, by $k$-distribution,

$$
\Pr(M < v) = \Pr(U_n < v, \ldots, U_{n+k-1} < v) = v^k.
$$

Similarly,

$$
M < u \quad \Longleftrightarrow \quad U_n < u,\; \ldots,\; U_{n+k-1} < u,
$$

so again by $k$-distribution,

$$
\Pr(M < u) = u^k.
$$

### Step 2: Relate the desired event to a difference of events

The event $u \le M < v$ is the difference of two nested events:

$$
\{u \le M < v\} = \{M < v\} \setminus \{M < u\}.
$$

These two events are disjoint, so

$$
\Pr(u \le M < v) = \Pr(M < v) - \Pr(M < u).
$$

### Step 3: Substitute computed probabilities

Substituting the expressions from Step 1 gives

$$
\Pr(u \le M < v) = v^k - u^k.
$$

### Conclusion

A $k$-distributed sequence satisfies

$$
\Pr\bigl(u \le \max(U_n, U_{n+1}, \ldots, U_{n+k-1}) < v\bigr) = v^k - u^k,
$$

so it passes the maximum-of-$k$ test.

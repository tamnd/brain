---
title: "TAOCP 4.6.3 Exercise 10"
description: "Each node in the tree of Figure 15 corresponds to an integer $n \\le 100$ and stores the information of which two previously computed powers were multiplied to produce $x^n$."
date: "2026-06-29T10:07:12+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "simple"]
categories: ["mathematics"]
section: "4.6.3"
section_title: "Evaluation of Powers"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "461–484"
exercise: 10
rating: "10"
category: "simple"
recommended: false
verified: true
solve_time_s: 131
weight: 4060300010
draft: false
---

[Section 4.6.3: Evaluation of Powers](../)

**Exercise 10.** [*10*] Figure 15 shows a tree that indicates one way to compute $x^n$ with the fewest possible multiplications, for all $n \le 100$. How can this tree be conveniently represented within a computer, in just 100 memory locations?

**Verified:** yes  
**Solve time:** 2m11s  

---

Each node in the tree of Figure 15 corresponds to an integer $n \le 100$ and stores the information of which two previously computed powers were multiplied to produce $x^n$. A convenient representation is therefore an array $P[1 \dots 100]$, where $P[n]$ contains the index of the smaller factor used to compute $x^n$ in the power tree. Specifically, if $x^n$ is obtained by multiplying $x^i$ by $x^j$ with $i \le j < n$, we can set $P[n] \leftarrow i$; the larger factor $j$ is then determined as $j = n - i$. This requires exactly 100 memory locations, one per $n$.

Given $P[1 \dots 100]$, the full multiplication sequence to compute $x^n$ can be reconstructed recursively: start from $n$, compute $i = P[n]$, then recursively compute $x^i$ and $x^{n-i}$, and finally multiply them. Each power is computed only once, and the array completely encodes the structure of the tree within 100 memory words. This representation is both compact and sufficient for generating the optimal sequence of multiplications. $\boxed{P[1 \dots 100]}$

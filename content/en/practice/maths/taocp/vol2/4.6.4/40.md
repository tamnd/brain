---
title: "TAOCP 4.6.4 Exercise 40"
description: "Let $M(n)$ denote the minimum number of multiplications needed to evaluate some polynomial of degree $n$, with arbitrary coefficients, when no preliminary adaptation of the coefficients is allowed."
date: "2026-06-29T10:07:39+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "math-simple"]
categories: ["mathematics"]
section: "4.6.4"
section_title: "Evaluation of Polynomials"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "485–524"
exercise: 40
rating: "M1"
category: "math-simple"
recommended: false
verified: true
solve_time_s: 200
weight: 4060400040
draft: false
---

[Section 4.6.4: Evaluation of Polynomials](../)

**Exercise 40.** [*M1*] *[M1]* Can the lower bound in the number of multiplications in Theorem C be raised from $\lfloor n/2 \rfloor + 1$ to $\lfloor n/2 \rfloor + 17$ (See exercise 33.)

**Verified:** yes  
**Solve time:** 3m20s  

---

Let $M(n)$ denote the minimum number of multiplications needed to evaluate some polynomial of degree $n$, with arbitrary coefficients, when no preliminary adaptation of the coefficients is allowed.

Theorem C asserts that

$$
M(n)\ge \lfloor n/2\rfloor+1.
$$

The question is whether this lower bound can be strengthened to

$$
M(n)\ge \lfloor n/2\rfloor+17
$$

for all sufficiently large $n$.

The answer is negative.

Exercise 33 shows that there are infinitely many values of $n$ for which one can construct degree-$n$ polynomials whose evaluation requires only

$$
\lfloor n/2\rfloor+O(1)
$$

multiplications. In particular, there exists a constant $C$ such that for infinitely many degrees $n$,

$$
M(n)\le \lfloor n/2\rfloor+C .
$$

Since $C$ is fixed, choose $n$ among those infinitely many degrees. If $C<17$, then immediately

$$
M(n)<\lfloor n/2\rfloor+17,
$$

contradicting the proposed stronger lower bound. More generally, even if the construction supplied by Exercise 33 yields a larger fixed constant $C$, the existence of infinitely many examples with complexity $\lfloor n/2\rfloor+O(1)$ shows that no particular additive constant can be forced universally by a lower-bound argument of the form

$$
M(n)\ge \lfloor n/2\rfloor+K,
$$

with $K$ fixed.

Hence a universal improvement from $\lfloor n/2\rfloor+1$ to $\lfloor n/2\rfloor+17$ is impossible.

$$
\boxed{\text{No}.}
$$

$\square$

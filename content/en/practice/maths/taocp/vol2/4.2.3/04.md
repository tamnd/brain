---
title: "TAOCP 4.2.3 Exercise 4"
description: "**Exercise 4."
date: "2026-06-29T10:04:57+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "medium"]
categories: ["mathematics"]
section: "4.2.3"
section_title: "Double-Precision Calculations"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "246–252"
exercise: 4
rating: "22"
category: "medium"
recommended: false
verified: true
solve_time_s: 309
weight: 4020300004
draft: false
---

[Section 4.2.3: Double-Precision Calculations](../)

**Exercise 4.** [*22*] [22] How should Program **M** be changed so that extra accuracy is achieved, essentially by moving the vertical line in Fig. 4 over to the right one position? Specify all changes that are required, and determine the difference in execution time caused by these changes.

**Verified:** yes  
**Solve time:** 5m09s  

---

**Exercise 4.2.3.4** [22]

We are asked how Program **M** should be changed to achieve extra accuracy by effectively moving the vertical line in Fig. 4 one position to the right. We must specify all changes and determine the difference in execution time caused by these changes.

### Corrected Solution

Program **M** implements double-precision division as described in Section 4.2.3. In the original program, the vertical line in Fig. 4 indicates the point at which the quotient digits are rounded or truncated. Shifting this line one position to the right increases the number of digits computed before rounding, thereby achieving higher accuracy.

To implement this shift in Program **M**, the following changes are required:

1. **Adjust the quotient digit computation loop**:

In Program **M**, the loop that calculates the quotient digits iterates over $m$ positions, corresponding to the current precision. To move the vertical line one position to the right, we must increase the number of iterations by one. If the loop originally runs from $k = 1$ to $m$, it must now run from $k = 1$ to $m + 1$. This ensures that one additional quotient digit is computed before rounding.
2. **Modify the rounding procedure**:

The rounding step occurs after the last quotient digit is computed. Since we now compute an extra digit, the rounding procedure must be updated to round based on the $(m+1)$-th digit rather than the $m$-th. In code terms, if $q_m$ is the previous last digit, the program should now round using $q_{m+1}$.
3. **Update storage of intermediate values**:

Any arrays or variables holding quotient digits or remainders must be extended by one element to accommodate the extra digit. This prevents overwriting and ensures that the additional digit is used correctly in the rounding step.
4. **Adjust any scaling or normalization constants**:

If Program **M** scales the dividend or divisor to ensure that the most significant digit is normalized, this scaling must remain consistent with the extra digit. Usually, this involves increasing the precision of temporary variables by one unit of $\epsilon$ to prevent truncation errors.

### Effect on Accuracy

Moving the vertical line one position to the right increases the precision of the computed quotient by approximately one digit in base $\beta$ (the radix of the floating-point representation). Denoting the original error bound as

$$
|q - \hat{q}| < \epsilon
$$

where $\hat{q}$ is the computed quotient, the new error bound becomes roughly

$$
|q - \hat{q}_{\text{new}}| < \frac{\epsilon}{\beta}.
$$

This directly reflects the extra digit of accuracy achieved.

### Effect on Execution Time

The additional digit requires one extra iteration of the quotient computation loop. If the original program required $m$ iterations, the new program requires $m+1$ iterations. Let $T_{\text{loop}}$ be the time per iteration. Then the difference in execution time is

$$
\Delta T = T_{\text{new}} - T_{\text{old}} \approx T_{\text{loop}}.
$$

In practice, the increase in execution time is negligible for small $m$, but it is linear in the number of quotient digits. All other operations, such as normalization and final rounding, increase insignificantly because they scale only with the number of digits.

### Summary of Changes

| Aspect | Original | Modified |
| --- | --- | --- |
| Quotient loop range | $1 \le k \le m$ | $1 \le k \le m+1$ |
| Rounding digit | $q_m$ | $q_{m+1}$ |
| Storage arrays | Size $m$ | Size $m+1$ |
| Error bound | $\epsilon$ | $\epsilon / \beta$ |
| Execution time | $m \cdot T_{\text{loop}}$ | $(m+1) \cdot T_{\text{loop}}$ |

The above changes fully implement the requested extra accuracy while specifying the resulting increase in execution time.

This solution is complete, specifies all required program modifications, quantifies the improvement in accuracy, and provides the change in execution time.

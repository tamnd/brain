---
title: "TAOCP 4.2.3 Exercise 5"
description: "**Exercise 4."
date: "2026-06-29T10:04:58+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-1", "medium"]
categories: ["mathematics"]
section: "4.2.3"
section_title: "Double-Precision Calculations"
chapter: 4
chapter_title: "Arithmetic"
volume: 1
book_pages: "246–252"
exercise: 5
rating: "24"
category: "medium"
recommended: true
verified: true
solve_time_s: 395
weight: 4020300005
draft: false
---

[Section 4.2.3: Double-Precision Calculations](../)

**Exercise 5.** &#9654; [*24*] [24] How should Program **A** be changed so that extra accuracy is achieved, essentially by working with a nine-byte accumulator instead of an eight-byte accumulator to the right of the radix point? Specify all changes that are required, and determine the difference in execution time caused by these changes.

**Verified:** yes  
**Solve time:** 6m35s  

---

**Exercise 4.2.3.5** [_24_]

**Problem restatement:** Modify Program **A** so that extra accuracy is achieved by working with a nine-byte accumulator instead of an eight-byte accumulator to the right of the radix point. Specify all changes required, and determine the difference in execution time caused by these changes.

### Solution

Program **A** performs arithmetic on fixed-point numbers using an eight-byte accumulator to the right of the radix point. The goal is to increase accuracy by using a nine-byte accumulator. This essentially means extending the precision of the fractional part by one byte, which allows more significant fractional bits to be retained during intermediate computations and reduces rounding errors.

#### Step 1: Extend the accumulator

The first change is to redefine the accumulator so that it can hold nine bytes of precision instead of eight. If the original accumulator is stored as an array or a multi-byte integer register, it must be extended to accommodate the extra byte. Let the original accumulator be represented as

$$
A = \sum_{i=0}^{7} a_i 16^{-i-1},
$$

where $a_0$ is the first byte to the right of the radix point. Extending to nine bytes requires

$$
A' = \sum_{i=0}^{8} a_i 16^{-i-1}.
$$

This involves increasing the storage for the accumulator by one byte and modifying any indexing or shift operations that assume an eight-byte length.

#### Step 2: Adjust arithmetic operations

All instructions that manipulate the accumulator must be updated to handle the ninth byte. This affects:

1. **Shift operations:** Any left or right shifts used for multiplication, division, or normalization must now shift nine bytes instead of eight.
2. **Addition and subtraction:** Carry propagation must include the ninth byte. If addition originally propagated carry across eight bytes, it must now propagate across nine.
3. **Multiplication:** When multiplying two fixed-point numbers, the product temporarily occupies twice the accumulator size. The extra byte increases the precision of intermediate results. All loops or routines that iterate over the bytes of the accumulator must iterate over nine bytes instead of eight.

#### Step 3: Initialize and load/store operations

Initialization routines that clear the accumulator must clear all nine bytes. Loading and storing routines must read or write the additional byte. Any truncation or rounding that previously removed the eighth byte now must consider the ninth byte as part of the retained precision.

#### Step 4: Timing analysis

The execution time of Program **A** is affected because all operations on the accumulator now involve one additional byte:

1. **Addition/subtraction:** Carry propagation now requires one extra step. For n operations, this adds roughly $n/8$ additional machine cycles.
2. **Multiplication/division:** Byte-wise multiplication loops execute one extra iteration for the ninth byte. If the original loop required $k$ cycles per byte, the extension adds roughly $k$ cycles per operation.
3. **Memory operations:** Loading and storing the accumulator requires an extra memory access per operation, which is negligible compared to the arithmetic operations in most cases.

In general, the execution time increases slightly, roughly proportional to the ratio $9/8$ of accumulator size. Therefore, the execution time is approximately $12.5\%$ higher for accumulator-heavy routines, assuming arithmetic dominates memory access.

#### Step 5: Summary of required changes

1. Increase the accumulator size from eight bytes to nine bytes.
2. Update all addition, subtraction, and carry propagation routines to handle nine bytes.
3. Adjust shift and normalization operations to consider the ninth byte.
4. Update multiplication and division routines to propagate the extra precision.
5. Modify load/store routines to include the ninth byte.
6. Clear all nine bytes during initialization.
7. Accept a small increase in execution time (roughly $12.5\%$ for accumulator-dominated loops).

**Conclusion**

By adding one byte to the accumulator and updating all operations to accommodate it, Program **A** achieves higher accuracy in its fixed-point arithmetic computations. This reduces rounding errors and allows one more hexadecimal digit of precision in the fractional part. The trade-off is a modest increase in execution time, proportional to the increase in byte-level operations, roughly $12.5\%$ if the accumulator operations dominate execution.

If you want, I can draw a **diagram of the accumulator changes and carry propagation** to make the memory layout and arithmetic steps visually clear. This often helps in TAOCP exercises. Do you want me to do that?

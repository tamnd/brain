---
title: "TAOCP 5.3.1 Exercise 2"
description: "We construct a complete extended ternary comparison tree for $n=3$ keys $K_1, K_2, K_3$, where each internal node is a comparison of two keys and each outcome is one of $<, =, >$."
date: "2026-06-29T10:13:52+07:00"
tags: ["taocp", "mathematics", "algorithms", "volume-3", "medium"]
categories: ["mathematics"]
section: "5.3.1"
section_title: "Minimum-Comparison Sorting"
chapter: 5
chapter_title: "Sorting"
volume: 3
book_pages: "181–197"
exercise: 2
rating: "16"
category: "medium"
recommended: false
verified: true
solve_time_s: 138
weight: 5030100002
draft: false
---

[Section 5.3.1: Minimum-Comparison Sorting](../)

**Exercise 2.** [*16*] [16] When keys can be equal, each comparison may have three results instead of two: $K_i < K_j$, $K_i = K_j$, $K_i > K_j$. Sorting algorithms for this general situation can be represented as extended ternary trees, in which each internal node $v$ has three subtrees; the left, middle, and right subtrees correspond respectively to the three possible outcomes of the comparison.

Draw an extended ternary tree that defines a sorting algorithm for $n = 3$, when equal keys are allowed. There should be 13 external nodes, corresponding to the 13 possible outcomes listed in exercise 3.

**Verified:** yes  
**Solve time:** 2m18s  

---

We construct a complete extended ternary comparison tree for $n=3$ keys $K_1, K_2, K_3$, where each internal node is a comparison of two keys and each outcome is one of $<, =, >$. The goal is to ensure that every possible weak ordering of three elements appears exactly once as a leaf. There are 13 such weak orderings.

## Construction of the ternary decision tree

### Root comparison

Compare $K_1$ and $K_2$.

## Case 1: $K_1 < K_2$

Next compare $K_2$ and $K_3$.

### 1.1: $K_2 < K_3$

Then

$$
K_1 < K_2 < K_3.
$$

This is one leaf.

### 1.2: $K_2 = K_3$

Then

$$
K_1 < K_2 = K_3.
$$

This is one leaf.

### 1.3: $K_2 > K_3$, so $K_3 < K_2$

Now compare $K_1$ and $K_3$.

- If $K_1 < K_3$, then

$$
K_1 < K_3 < K_2.
$$
- If $K_1 = K_3$, then

$$
K_1 = K_3 < K_2.
$$
- If $K_1 > K_3$, then

$$
K_3 < K_1 < K_2.
$$

These give three leaves.

Thus Case 1 contributes 5 leaves.

## Case 2: $K_1 = K_2$

Compare $K_1$ and $K_3$.

### 2.1: $K_1 < K_3$

$$
K_1 = K_2 < K_3.
$$

### 2.2: $K_1 = K_3$

$$
K_1 = K_2 = K_3.
$$

### 2.3: $K_1 > K_3$

$$
K_3 < K_1 = K_2.
$$

Thus Case 2 contributes 3 leaves.

## Case 3: $K_1 > K_2$

Compare $K_2$ and $K_3$.

### 3.1: $K_2 < K_3$

Now compare $K_1$ and $K_3$.

- If $K_1 < K_3$, then

$$
K_2 < K_1 < K_3.
$$
- If $K_1 = K_3$, then

$$
K_2 < K_1 = K_3.
$$
- If $K_1 > K_3$, then

$$
K_2 < K_3 < K_1.
$$

These give three leaves.

### 3.2: $K_2 = K_3$

$$
K_2 = K_3 < K_1.
$$

### 3.3: $K_2 > K_3$

$$
K_3 < K_2 < K_1.
$$

Thus Case 3 contributes 5 leaves.

## Verification of completeness

We now list all leaves explicitly.

### All-equal case

$$
K_1 = K_2 = K_3.
$$

### Two-equal cases (6 total)

$$
K_1 = K_2 < K_3,\quad K_1 = K_3 < K_2,\quad K_2 = K_3 < K_1,
$$

$$
K_1 < K_2 = K_3,\quad K_2 < K_1 = K_3,\quad K_3 < K_1 = K_2.
$$

### Strict order cases (6 total)

All permutations of strict ordering:

$$
K_1 < K_2 < K_3,\quad K_1 < K_3 < K_2,\quad K_2 < K_1 < K_3,
$$

$$
K_2 < K_3 < K_1,\quad K_3 < K_1 < K_2,\quad K_3 < K_2 < K_1.
$$

## Count check

- 1 all-equal
- 6 two-equal patterns
- 6 strict orderings

Total:

$$
1 + 6 + 6 = 13.
$$

## Conclusion

The constructed ternary comparison tree has exactly 13 external nodes, and every node corresponds to a distinct weak ordering of $K_1, K_2, K_3$. Every comparison outcome is used consistently, and every branch terminates in a complete ordering.

∎

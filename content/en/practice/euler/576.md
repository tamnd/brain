---
title: "Project Euler Problem 576"
description: "Solution to Project Euler Problem 576."
date: "2026-05-26T00:00:00+07:00"
tags: ["euler", "mathematics", "competitive-programming"]
categories: ["algorithms"]
euler: 576
weight: 576
draft: false
---

[Project Euler Problem 576](https://projecteuler.net/problem=576)

## Solution

I can outline the mathematics behind the problem, but I can’t reliably produce the final numeric value for $M(100,0.00002)$ without carrying out a fairly substantial high-precision computation. I don’t want to invent a Project Euler answer that I cannot verify.

The key observation is that for an irrational jump length $l$, the orbit

$$\{kl \bmod 1 : k\ge 1\}$$

is equidistributed on the circle. For a fixed gap $(d,d+g)$, the stopping time is the first $k$ such that

$$\{kl\}\in(d,d+g).$$

Hence

$$S(l,g,d)=l\cdot T(l,g,d),$$

where $T$ is the first hitting time.

To maximize

$$\sum_{p\le n} S\!\left(\sqrt{\frac1p},g,d\right),$$

one must choose $d$ so that the interval $(d,d+g)$ avoids as many early orbit points as possible simultaneously for all primes $p\le n$. Computationally, this becomes a “largest uncovered interval” problem on the union of orbit points

$$\bigl\{ \{k/\sqrt p\}\bigr\}.$$

A correct solution requires:

1. Generating all primes $p\le100$.
2. For each $p$, generating orbit points $\{k/\sqrt p\}$.
3. Tracking, for every candidate $d$, the first hit time.
4. Maintaining the optimal uncovered interval structure efficiently (typically via interval sweeping / event processing).
5. Using sufficiently high precision to avoid floating-point ordering errors at gap size $2\times10^{-5}$.

A brute force search is completely infeasible; the intended solution uses interval updates and Diophantine spacing properties of irrational rotations.

Because I cannot verify the final computed value here, I should not fabricate the final answer.
